# DIALOGUE — what the Design Table has agreed so far

Rewritten, not appended, whenever the Table moves. Kept under 150 lines. The Design Table issue thread (#17) is the archive.

## Agreed

### First design round (2026-09-14, both partners; DECISIONS/0010 for the killed experiment)

Chat's full reply to Code's opening post, and Code's answer. Recorded in DESIGN.md and in the acceptance lines of issues 3, 5, 10, 12, 16.

- **The forecast shows the resolved probability.** Whatever roll scheme is in use, the forecast prints the probability the resolver will actually land, rounded to the nearest integer, never the raw hit stat. Forecast, resolver, and AI scorer call one hit-probability function; the scheme is a switch inside it. Issue 5's 100k-trial test compares displayed against realized. (DESIGN section 5.)
- **Guard groups wake by proximity, not rectangles.** One global wake radius of 4 tiles, Manhattan, walls not considered, identical on every map, stored in content and never in a map file, so the player learns it once and counting tiles is a skill. Noise wakes at radius + 2. Any member's death wakes the group at any distance. Waking emits an event naming the cause. The check runs after every player command, on where units stand. `trigger:` is gone from section 10 and issue 3. (DESIGN section 8; issue 10.)
- **Recall scars (13.3) is killed.** It taxed the recruit's future instead of the charge, and its rational play was to let the recruit die; it also told a story the player's own timeline contradicts. (DECISIONS/0010.)
- **Recall restores the rolls**, as the intended rule: the same attack after a Recall gets the same rolls, so a Recall lets you choose differently, never reroll. True only once rolls are keyed (issue 31); a sequential stream lets the player launder a reroll by reordering unrelated actions. Issue 31 is not a fork and lands with issue 5. (DESIGN section 7.)
- **Experiment order.** Map events first, as Phase 2 infrastructure (issue 32), not a spike. Enemy retreat second (issue 33) with the no-chase constraint: retreat only to a fort reachable this turn, never twice. Rapport and Rivalry third (issue 16). Commander's Word was deferred until maps 1 to 3 and the hit A/B; the sixth round superseded that with its number, #85, after map 8.
- **Gate 4 is ablation, not kills.** Bench each recruit, replay the same seeds, compare paired by seed. Code's amendment: the threshold is relative (a recruit is dead weight under half the median drop), because every bench removes a body and a body always matters. Provisional. (DESIGN section 11; issue 12.)
- **Gate 3 may be waived from map 4 on**, declared in the map file as `cheap_shots: allowed` and printed by the Sim, never exempted quietly in a test. **Map authoring.** Armored's corridor job needs one-tile corridors; with no zone of control a two-tile gap is no wall. The map format is written by the game as well as read, for 13.5. (DESIGN sections 9 and 10.)

### Second round (2026-09-14, both partners)

Chat's reply to Code's answer, and Code's answer to it. Recorded in DESIGN.md sections 3, 5, 8, 11, and 13.1, and in issues 12, 16, and 31.

- **The "three places" argument against two rolls is withdrawn.** One hit function, three callers, under either scheme. Only the terrain reason stands.
- **Gate 4 depends on issue 31.** The ablation is a paired experiment only once rolls and growth are keyed: under a sequential stream, benching one unit reshuffles every later roll and every other recruit's growth. Keying is common random numbers; it does not cut the run count (the baseline is gate 1's seeds), it cuts the variance. Chat's 200-seeds-per-bench figure was the un-keyed cost. (Issue 12; DESIGN section 11.) Growth rolls are keyed on (unit, new level, stat) and nothing else. Two reasons agreeing: it kills level-up scumming through Recall, and it makes every recruit's level-ups invariant under ablation. Keying on map or turn would reopen laundering and break the gate. A starved stat on one recruit is a character; the Sim shows the spread before anyone argues for a change. (DESIGN section 3; issue 31.)
- **Gate 4 fails only past a noise margin and prints the action mix.** Report each drop with a standard error; fail on `drop + 2 * SE < 0.5 * median`. At 200 seeds the margin is about the threshold's size, so the gate catches near-zero recruits and nothing subtler, which is what dead weight means. Each recruit's action mix per arm (attacks, damage, heals, attacks absorbed) prints next to the drop so a healer failure can be read as a heuristic failure without re-deriving it. (Issue 12; DESIGN section 11.)
- **The wake rule is "do not stop close."** Manhattan, walls not considered, evaluated on where units stand after each player command. A unit can run past a sleeping group and end beyond the radius; that is a designed play and Cavalry's first job that is not arriving early. The noise radius prices a dash that fights on the way through. (DESIGN section 8.)
- **Rivalry keeps the symmetric-cost lean as the second arm, and gains a third.** Chat checked the numbers against the starter content: both sides move about ten points, so the cost is symmetric in probability, which is the real defence. `CritAvoid` is not clamped, and a test falsifies the clamp. The hole: the bonus rides every attack made, the cost only attacks received, and good play drives the cost to zero. The spike logs exposure (fraction of rival-adjacent player-phase actions followed by an attack on that unit); if it is low, the third arm is a counter-only bonus. All arms are data changes. (DESIGN sections 5 and 13.1; issue 16.)
- **The enemy AI prices crit.** Code's addition: section 8's expected damage includes the crit expectation, computed by the same functions the forecast uses, so a lowered crit avoid makes a unit a better target in the enemy's own arithmetic rather than a cost the enemy cannot see. Without this, exposure would measure geometry only. Builder-level call; argue it on issue 10's PR.

### Third round (2026-09-16, the Critic's first pass at e1cdf91, Chat, Code; issue 47; every number run by two of the three)

- **Section 11 has no instrument for a dead approach.** Gates 1 and 2 integrate over the whole map, so a map passes both while its opening is a walk. Old Mill Road turn 1: 21 destinations from the captain at 1,8, all Plain, nothing attackable from any of them, and no enemy able to answer. Pillar 1's claim is per turn and section 11 tests it once per map. The Critic's doubt is upheld.
- **Dead turns are measured and printed in `--full`, never gated on.** A turn is dead when no player unit can reach a tile from which it can attack, and no enemy can reach a tile from which it can attack a player unit. A sleeping Guard group threatens nothing, so the approach past one reads dead; that false positive is accepted and named rather than hidden by computing on nominal Mov. Counters: dead turn count, longest leading run, turn of first contact. (Issue 47.)
- **The free prefix is the ground truth (issue 47).** Ablation over turns, the shape of gate 4: play turns 1 to N with the random player, hand the run to the heuristic at N+1, compare the win rate to gate 1's baseline paired by seed. The free prefix is the largest N indistinguishable from baseline. N in {2, 4, 6} on gate 1's seeds, 600 runs per map. The pairing is weaker than gate 4's (a random prefix decorrelates the board at the first differing command; issue 31 still buys identical growth and setup). The dead-turn counter is the diagnostic that says why a prefix is free. Gate 9, when it is written, is written against the free prefix, only after maps 1 to 3 have produced both numbers and their journals. Nothing is wired to an exit code until then.
- **The dash past a sleeping group is real, it is cavalry's, and Mov is its dial, not the radius.** Turn-saving dashes past a radius-4 group on open ground: 240 at Mov 6, 128 at Mov 5, 64 at Mov 4, the Mov-6 count constant at every radius from 3 to 8, so a radius change only lengthens the approach; section 8 stands. The noise clause prices most of them (quiet-and-armed dashes: 8, 24, 64 of those), and a group in a corridor cannot be dashed past, only woken (Critic). Facts for issue 13's authoring.

### Third round, second pass (2026-09-16, Chat's amendments to Code's two additions; issue 47)

Code's two leans on issue 47 are accepted, each with one amendment, and both are in issue 47's body. Nothing in 47 is wired to an exit code.

- **The free prefix prints beside `turn_limit - p90(winning turn)` as well as gate 1's median slack**, since win rate moves when the seeds near the limit flip, the right tail, and against the median alone a tempo-only failure reads as content in the direction that flatters the map; the p90 is the 200-seed quantile (about `1 - 2 * SE / w`, 0.9 at a 70 percent baseline). **At the boundary N only, the prefix arm re-runs with the turn limit raised by N** against the unraised baseline: a drop that survives the refund is positional, passing under it means at most tempo. About 200 runs, none on a map already free at the largest N.
- **Dead, quiet, live, with the wake tax printed beside the state.** Quiet is a binary and a binary that fires the same way on very different turns cannot be checked against a journal, the objection to the dead-turn counter turned on its author. So print the overlap of the reach set with a sleeping group's radius, count and fraction, taken from the player unit with the largest fraction. The leading run counts dead only. `turn of first quiet` is reported against a tax floor whose value waits on issue 13's journals. Denominator convention: the reach set as section 4 defines it, own tile included, since Wait is one of the tax-free choices; Chat's table below excludes it, which is why its totals run one short of `ironwake reach`.
- **Old Mill Road's wake tax at e1cdf91** runs from 0 of 24 at 2,8 to 15 of 35 at 7,4 for infantry, reproduced by Code; 6,6 and 7,4 are both quiet by the bare rule and are not the same turn. **Promotion, sharpened:** an instrument is validated by a map where its number is non-zero and the journal disagrees with it; agreement on a silent instrument is worth nothing.

### Fourth round (2026-09-16, the Critic's recommendation, Code, Chat; the front-loaded block)

- The Critic said we agreed play settles what reasoning cannot and then scheduled play ninth. Code applied the lean; Chat lengthened the block by one issue. All built. **Play came before EXP and inventory:** after the Critic's bugs, 5 with 31, 6, 7, then 10 and 11 (10 first, the order the priority rule produces by itself), then 8 and 9. Without 10, three of Old Mill Road's four enemies behave as they would with no AI at all, so 11 without 10 would have been a walk. The first two journal entries were systems entries, not Fun Gate entries (Old Mill Road is a format sample; issue 13 had not run), and the first run of the finished build was a debug pass: a scripted approach with no player attacks, the prediction committed ahead of the run as a pair under `docs/transcripts/`, agreement not the acceptance (Chat's mechanics, issue 11's body).

### Fifth round (2026-09-16, Chat's approach rule; issue 10, DESIGN section 8)

- **Section 8 had no approach rule.** Its tile rule is scoped to tiles that allow an attack, so on a turn when an Aggressive unit can attack nobody the only governing text was "moves toward", and the debug pass is exactly those turns. Predictable, not merely deterministic: an implementation-ordered pick among tied tiles replays identically and breaks DECISIONS/0012's contract, so 10's acceptance carries the Old Mill Road case as a named test. Written into section 8 ahead of 10's PR; Code's three debug-pass additions stand (the script in the prediction's commit; the prediction names its seed; a fresh pair inside the `bug` PR that fixes 10).
- **The rule (Chat's, agreed):** target is the nearest player unit by path cost to an attack tile, ties by unit id; destination minimizes remaining path cost to an attack tile on that target; ties by avoid, then fewest player units whose reach set contains the tile, then (cost, row-major). Path cost, never Manhattan. Code's additions: no path means not a target and no target means Wait; "can reach the tile" in both tile rules means reach-set membership, so `ironwake reach` checks it; enemy units act in ascending unit id.
- **On Old Mill Road the tie is three-way and the rule says 3,6.** Chat's table derived 4,7; Code's Dijkstra finds 3,6 tied too, and row-major takes y=6. Two readers of one document, one wrong, caught by the third computation rather than by the run.

### Sixth round (2026-09-17, the Phase 3 backlog, Cha's jobs, pacing; issues 66 to 85)

Code's scheduling note (Lotus, by way of Code: Builder runs work the queue for about 50 minutes), Chat's filing of the Phase 3 backlog, and Code's answer.

- **The Phase 3 backlog is filed, #66 to #83, and the numbers are the build order.** Chat filed them in dependency order because the priority rule takes the lowest number inside a phase. #77 (supports) and #83 (map 8) carry `blocked` with the unblock condition in their bodies. Five leans in the bodies are Chat's, provisional, argued on the PRs. Battalions are the one section-12 system not filed (below).
- **Builder pacing, agreed, with two things protected.** At several issues a night what slips first is step 3 (the trace against DESIGN.md before code) and the decision record, and step 3 is what caught the spec errors so far. A run that must choose between another issue and tracing the current one ships one fewer; ROUTINES.md's Builder prompt carries the line. The Critic's cadence should follow the merge rate (it runs twice a week, not weekly): proposed to Lotus, nightly at 06:00 after the fourth Builder run while the run cap allows.
- **The order after 11 is agreed as written:** the two cold systems entries, the hit A/B, then the Fun Gate on the three maps.
- **Enemy template scaling uses effective growth, clamped per stat to 0..100** (issue 44's question; #84, `bug`, before 6 builds `BattleState` from `EnemyUnit`). The deterministic form equals the rolled level-up in expectation only against the growth the rolls use, and a template raised to a level must match one authored there, so enemy numbers derive from the class the glyph shows. On the starter content the difference is one point per affected stat at four levels gained and no clamp fires; the rule is the point.
- **Gambits as section 12 names them are the wrong shape** (Chat; Code agrees). One obviously correct move against pillar 1; a second riskless per-map charge beside Recall, so tension gets priced by the cheaper one; an equipment layer whose decision is which stat block on which unit. Battalions stay in section 12 unfiled; dropping them is a `fork` for Lotus with both positions, filed once either Cha job below has been played, not on reasoning alone. Section 12 says so, so the drop cannot happen by omission (trigger and deadline settled in the round's last exchange, below). Code's addition: the one thing gambits had that nothing else has is the stun that turns a boss into a pile-on; if the journals say bosses are walls, that is a fourth captain's order, never an equipment layer.
- **Cha's two v1 jobs, agreed as the lean; both spikes, both content.** The captain's Cha scales Commander's Word (13.2, #85). A recruit's Cha sets the rate rapport accrues (13.1, issue 16's spike), printed on the unit screen as the rate, never a bare Cha. Chat's deadline is met without waiting: issue 13 writes Cha as character, the captain heavy and the recruits spread, and both jobs are dials over that spread. Commander's Word: arm B is the expected shape, both partners. Arm A, as written: magnitude by Cha, reaches everyone. Arm B: magnitude fixed, reach is a radius from the captain in tiles set by Cha, Manhattan, the wake rule's distance. Chat withdrew arm A as its lean; #85 still runs both, since they are two content rows, but the record says arm B is the one we expect to keep, not that the partners are split. Chat's reason, which neither of Code's two were: a magnitude scaled by Cha is honest after the fact (the forecast shows the resolved avoid) and useless before it, since the player cannot price the order until it is spent, and a radius is countable at the only moment the number is a decision. Code's pricing reason stands too: the captain is the loss condition, and an order that reaches everyone from the back rank is the second riskless charge Chat objected to in gambits. Under either arm the order is the captain's action for the turn. After #66 and the hit A/B.
- **Arm B's failure mode is Cha going idle, and #85 logs the number that says so:** the binding fraction per order (recruits reached over recruits alive, captain excluded, per map, never pooled), the keep condition being that it is below 1 often enough to be a choice; the curve starts at or below 4 (`Cha / 4` up to Cha 16) so the captain's first diamond is the one the wake rule taught; #85 runs after map 8 because the sample parties are packed within Manhattan 2 and the radius would never bind. The A/B's confound (arm B drags the captain forward, so fewer calls cannot tell a tight radius from a dead captain) is read by logging whether the captain was attacked after the order, rivalry's exposure counter over a different subject. **The rapport rate has a sign the spike must read** (a high-Cha recruit loses the show-off bonus soonest; the sign follows the rivalry arm, #77's own blocked reason), and issue 16's body carries the rate, one provisional overwrite threshold, and the journal question. **The battalions fork** (Chat's audit, applied): the trigger is either Cha job played, kept or killed; the PR carrying that spike's decision record files it (issue 16 by the queue); the deadline is #74, `blocked` until the fork is resolved, which also holds #82 and #83, and a "wait for #85" ruling is available to Lotus.

### Seventh round (2026-09-18, Chat's cold entry on the Tollgate; the spent spell, turn limits, the Hold boss on a Seize map)

Chat's first cold play (the Tollgate, seed 23, five bodies; `docs/transcripts/2026-09-18-the_tollgate-23.txt`, entry in PLAYTEST.md) and Code's answer. Both cold systems entries now exist; the hit A/B is next by the fourth round's order, once issue 12 lets the Sim fight a map.

- **The spent spell stays as called** (DECISIONS/0018 item 3; section 5 as written). "Never helpless" is about steel. Chat's reasons, in order of weight: with no weapon triangle, five casts against forty swings is the texture the weapon classes have in v1; unarmed is not actionless (a body blocks a corridor, takes a dressing, and the enemy scorer prices a unit that cannot counter as free damage, which is a problem to solve rather than a dead turn); a spell with no uses is no spell. Two conditions, content and display, not rules: **no Reason or Faith unit ships with one casting option** (issue 13's acceptance; a second book is the only sidearm an Adept can carry, and a mage choosing between two books that differ in Mt, hit and uses is the first real user of #99's slot argument), and **an unarmed unit says "unarmed"** wherever its line is drawn, since seeing it coming is the player's whole defence (folded into #101).
- **Turn limits on the sample maps do not bite.** Neither partner has been hurried by a map: 20 with 8 to spare on Old Mill Road, 16 with 4 to spare on the Tollgate after three turns of walking. A limit that never binds is a number the player never reads. Agreed: issue 13 tunes every map's limit to the honest line plus about two and lets the first miss be a real loss. Code's addition: the honest line has two instruments that must agree, the hand plays and gate 1's p90 winning turn (issue 47 already prints `turn_limit - p90`), and a limit at p90 plus two still leaves gate 1's 60 percent inside it, so the two readings are one rule.
- **A Hold boss beside the throne on a Seize map is a feature**, not the bookkeeping Code found on a Rout map. Because the win is the captain on the tile, killing the boss is optional and visibly so; on the Tollgate the choice was a free archer kill against a 60 EXP boss kill that cost 17 HP a dressing buys back, at the same turn count. A wager, not a dilemma, because the clock was not pressing; make the limit bind and the same turn is section 13's "two good moves and one of them is wrong", arriving from the win condition and the clock with nothing new to author. Noted as the shape for map 5 (#79).
- **Five bodies changed what the systems ask.** The corridor made the decision *which* unit rather than *how many*; the item action's real shape is that a move and a dressing are one turn, a tempo move with a heal attached; the bow's dead zone read as a tactic (a unit adjacent to an archer is safe from it); the wake rule's noise half taught itself only when the proximity half was already learned. Facts for issue 13's authoring. Durability still did nothing over twelve turns; it is a campaign number. Recall's first spend read as neither cheat nor shame. The rewind gave back the situation, not the dice (a different actor, different keys, #31 working as intended). What it cost was the interface: `recall <n>` over 92 states with no index printed anywhere. Evidence for #75, not a rule change.
- **Script mode continues past a rejection and that is a bug in the witness** (#101, Chat's filing). Six of 85 lines rejected, three of them silently costing a unit its turn. A rejection summary at the end of a run and `--strict` for the test corpus and the debug pairs; interactive play unchanged.
- **Issue 12's two leans stand** (a bench leaves the placement empty; `HeuristicPlayer` is its own planner over `Score` and `AttackTiles`) **and the captain veto is arithmetic, not categorical** (Chat's amendment on #12, agreed): refuse a tile when the summed worst case of every enemy that can reach it, doubles and crits included at the forecast's own numbers, is at least the captain's HP; a categorical veto could never take the Tollgate's 6,3, which is in every enemy's reach set by construction and is the map's solution. Code's refinement, on #12: one sum over the whole cycle, the worst-case counter on his own attack plus the enemy phase, against current HP, since two halves that each pass can add to a death. Same reach sets as the exposure counter in #85 and issue 16; three callers, one function. Narrowed in the tenth round: an enemy the named attack kills with certainty is not counted (#117).

### Seventh round, second pass (2026-09-18, Chat's reply to Code's three additions; the veto's crit term on #12)

Nothing argued. Three dependencies and one code trace, all agreed; Chat traced the planner and Code read the same lines.

- **The turn limit is one rule read from two instruments, and the veto is settled before issue 13 reads either.** Gate 1's p90 is a p90 over the heuristic baseline, and the baseline's willingness to commit the captain is what the veto decides. A veto that refuses too much lengthens p90, loosens every limit, and does it invisibly, in the direction the maps are already wrong in. Issue 12 lands before 13 by the queue, which is the plan.
- **The captain veto refuses on the no-crit worst case and orders on the crit-inclusive one** (Chat's finding on #12, agreed). Iron weapons carry crit 0, so the Tollgate's 6,3 passes the one-sum veto only because the crit term happens to be zero; a hatchet at 2 percent makes the tile beside the boss 30 against 22, a boss the captain may never stand next to at any HP, the categorical veto back through the multiplier, and it starts biting on maps 5 to 8 where steel and a higher `enemy_level` are the point. The exposure query returns both sums. The hard refuse reads every strike landing with no crit (the failure that actually happened in play, 5 plus 8 against 13); among tiles that pass, one where a crit cannot kill is preferred, as the first key before section 8's tile keys. No threshold percentage: a tuning constant nobody could defend. The same pair is the forecast's future line, "worst case 21 of 22; a crit from the lance makes it 37".
- **The enemy planner already knows an unarmed unit and is taught nothing more.** `EnemyAi.Score` prices the counter through `Combat.Forecast`; a spent spell leaves `EquippedWeapon` null and `CanStrike` false, so the option takes `NoCounterBonus`. A 5-damage strike on a unit that cannot counter prices about 15 against about 3.7 for an 8-for-8 trade under two rolls, so the magnet is real, needs no new term, and ranks below a kill only (`KillBonus` 100). `Approach` stays score-blind as section 8 writes it: a score-reading approach is pursuit, which turns running dry from a cost into a death sentence, makes enemy movement depend on inventory at a distance, and makes gate 1's number less about the map. Consequence: #101's `unarmed` line is the tell for a behaviour that already ships, not cosmetics.
- **Maps are authored at about one contested tile per two deployed units, or deploy fewer.** One contested tile poses one question per cycle however many bodies stand behind it; the Tollgate deployed five, asked "which" once, and three walked. Deployed minus twice the contested tiles is how many units walk, and issue 47's dead-turn count per unit is the instrument that reads it off the Sim. The sentence sits beside "which unit rather than how many" in issue 13's body. Code's gloss: on open ground a contested place is a threat frontier rather than a gap, and it moves each turn; the count is of places worth standing, not of wall gaps.

### Eighth round (2026-09-19, Chat's three answers on PR #104; issue 105, DECISIONS/0020)

Chat built 14cedea and ran issue 12's three Unsure questions rather than reasoning about them; all three are no, and Code agreed. Recorded in DESIGN section 11.4 and DECISIONS/0020.

- **The heuristic gets no healing-terrain sense.** The lone captain on Old Mill Road refuses the boss at full HP (32 against a ceiling of 22 on every adjacent tile), so a fort would move where he waits and change nothing. The Def and Res term inside the exposure sum is the terrain sense the baseline is allowed; `Approach` stays score-blind for the same reason as the seventh round.
- **Old Mill Road's 40 percent is a content finding for issue 13, not a baseline defect.** Two cadets kill that boss in one phase and one cannot kill it at all, so the map ends when the second body dies and then runs fourteen turns without saying so. When 13 retunes: the boss is beatable by one cadet, or losing the second body loses the map then and there.
- **Recruits take no veto**, on the design reason as well as the numbers: the captain's refusal is a rule because his death is the loss condition; a recruit's would be a policy, and a baseline with policies measures itself instead of the map, the confound section 11.4 exists to avoid.
- **Gate 4 gains a map-level verdict at a median drop of zero or below**, and the per-recruit rule applies only above zero. The verdict is gate 4's, not issue 47's; it is the gate-side instrument for one contested place per two deployed, and section 11.4 drops "a body always matters" as falsified by the Tollgate run.
- **Gate 4's row compares the rest of the cast against itself across the bench** (three mixes: the recruit's own, the rest's baseline, the rest's benched) and prints the captain's mix as unjudged data. Chat's prediction to read off it: on maps 5 to 8 the veto makes the captain a piece to position rather than swing, which is the strongest argument yet for Commander's Word (#85).

### Ninth round (2026-09-19, the hit A/B on the cast; casters, the cast verdict; issues 113 to 115, DECISIONS/0023)

Chat ran both arms on the real cast at 93c4dec (`--full --all --scheme two` against `one`, the same 200 seeds) and Code reproduced every row; both partners' arithmetic on the content agrees to the point.

- **The hit A/B is settled: two-roll averaging, the default.** Old Mill Road 65 to 60 percent (one standard error, nothing), Saltmarsh Ford 85 to 63 (five), the Tollgate 99 on both. The finding is gate 4's median drop as a share of the baseline's wins: 95 to 77 percent on Old Mill Road, 82 to 59 on Saltmarsh, so under two rolls who you brought explains more of the outcome and under one roll the dice do, round one's criterion pointing the other way from both partners' lean. The obvious explanation is wrong and stays on the record: the cast's raw hit has median 92, the enemies' 86, both at the top of the band, so one roll widens the player's edge (6 points against 2.6); the cost is that both sides miss more, fights run longer, and only the player is on a clock (Saltmarsh's winning p90 12 to 16 against 18). Chat's terrain reason is withdrawn as overstated: two-roll averaging makes forest's 20 worth less than printed only above raw 85 (11.0 at 96 to 76, 19.0 at 86 to 66, 33.3 at 68 to 48), a fort above about 90, and the median matchup is raw 86. The "three places" reason was retired by the build (one hit function; gate 5 draws its scheme per combat). DESIGN section 5 says so.
- **`--scheme` stays and issue 15 carries the reopen condition:** if either partner's Fun Gate journal says the turns read as arithmetic rather than risk, one roll is the first dial, a one-word experiment. The flip instrument from the old leaning entry is unfiled; the ablation share answered its question from the other side.
- **A Reason or Faith cast member carries at least one unconditional casting option** (#113), not merely two castable spells: a healing spell is legal only when an ally in range is hurt, so `salve` plus `beacon` passes the loader's count and leaves a chaplain with Move and Wait as the cheapest target on the board. Maud as shipped passes. Code's lean, not agreed: Radiance keeps its 5 uses into run 2, since a healer choosing whether to spend one of five bolts is the spent-spell rule doing the job we kept it for; the journals say whether that is an arc or a trap.
- **Gate 4's cast verdict fails unless the median drop is above zero by twice the median row's standard error** (#115), with two lines: the confidently-wrong case (benching raises the win rate) and the ceiling case (the median recruit changes no outcomes). The Tollgate flipped from FAILED at -0.003 to ok at +0.015 on the arm alone, every row but Pell inside one standard error of zero, so the rule as built read the sign of noise. The two margins point opposite ways on purpose: the per-recruit one guards a named recruit against a false accusation, the cast one guards a map that measured nothing against being recorded as tuned (into DECISIONS/0020, item 6; built, the Tollgate failing on the ceiling line under both schemes). The Tollgate's failure is the map's: one contested tile with five bodies behind it.
- **Content findings for run 2, not formula changes:** the enemy raw-hit band against the cast comes down a few points rather than up (57 of 99 matchups sit above forest's break-even); Dunstan, the corridor unit, is the easiest unit in the cast to hit by both kinds (raw 96 from `archer`, 94 from `hexer`), armor working against steel and a Res 1 hole against magic. In issue 13's body.

### Tenth round (2026-09-19, Chat's loss breakdown; the veto refuses a certain kill; issues 117 and 118, DECISIONS/0024)

- **The sample maps do not lose, they stall, and always against the boss.** 197 of 197 timeout losses across three maps and both schemes end with `bandit_leader-1` alone alive after a mean quiet tail of 15.0 turns of Old Mill Road's 20; refunding the clock converts nothing on two maps and 14 games on Saltmarsh under one roll. #114 prints the breakdown and the quiet tail, constant-free.
- **The veto refuses a kill it cannot miss (#117, `bug`, agreed; DECISIONS/0024).** A worst-case veto compares a tile against a threshold and never against the alternative. A target the named attack kills with certainty contributes nothing to either sum; certainty is the raw hit at 100 (under two rolls raw 95 to 99 all print 100, so "displayed 100" is a threshold that changes width with the scheme) with the first strike's damage at the target's HP. Built: Old Mill Road 129 to 184 under two rolls, 120 to 164 under one; Saltmarsh Ford and the Tollgate unmoved, since their stalls end with the boss at full HP, a judgment about risk and a content finding.
- **Radiance keeps 5 uses into run 2** (agreed); #113 stays a load-time check. No current map deploys Maud, Dunstan, or a magic-using enemy, so the caster arc, the corridor job, and Res are first tested in run 2.
- **`--trace` did not replay in the CLI when it used an item (#118, `bug`).** Built: one-based slots and `# enemy:` comments; the seed-2 trace replays to its win under `--strict` in a test.

### Eleventh round (2026-09-19, Chat's re-measure of #117; survival weighting against the certainty narrowing; issues 125 and 126)

Chat re-measured #117 under the exact condition at 57c04ce before the PR did, then proposed weighting the target's two terms by its survival in place of the narrowing. Code patched the weighting on a scratch tree (never pushed) and ran both schemes with the loss breakdown and a probe on every captain attack the weighting passed and the worst case refused.

- **#117 as built is agreed in full.** `HitChance` at 100, never `DisplayedHit`; the first strike's damage at the target's HP, never the double; raw 99 under two rolls is the falsifier. The exact condition does not meet the reopen line: Old Mill Road 184/200 (p90 7) and 164 under one roll, identical to the loose condition, so no seed had the captain at raw 95 to 99 against the boss. No round owed.
- **The two conditions diverge under gate 1, on gate 4 (Chat's finding, reproduced).** The Tollgate's captain-boss boards enter the printed-100 band at raw 98 and 99 while Old Mill Road's decisive board is raw 100, so Pell's paired drop reads 0.440 under the exact condition and about 0.07 under the loose one, eight standard errors on one or two raw points of the captain's hit. A recruit whose drop is rescuing the captain from his veto is an instrument reading on any veto that is not a person. Pell's row (0.440 under two rolls, 0.170 under one) goes into issue 13's body as a number not to author against until the Fun Gate has played the Tollgate.
- **Survival weighting is not taken; the no-crit sum stays a pure worst case for the target's two terms (Code's lean, argued once with numbers; play settles it from here).** The weighting is an expected-damage sum read against a hard HP, and an expectation against a threshold passes boards where the death is a coin flip. Of the captain's attacks it passed that the worst case refused, 24 of 182 on Saltmarsh Ford under one roll had a kill probability under 50 percent; captain deaths rose 18 to 26 there, and Saltmarsh's Wren row went 0.605 to 0.095, the map FAILED, the Pell shape on the map that carries the hit A/B. Gate 1 wins match Chat's within two games. A veto is a guarantee or it is a policy, and a baseline with policies measures itself (seventh round); the step at 100 is the one place a step is honest.
- **The disease gets an instrument, not a new veto (#125).** Gate 1's row prints, over its timeout losses, the median of the highest kill probability the veto refused in the game, beside the quiet tail; gate 4's rows print the same over the benched arm's timeouts. Constant-free, and it reads a veto-rescue row (Pell at 0.99) apart from a risk stall (a boss at full HP) without touching the rule. Chat's band histogram as a standing number. **A strike at raw hit 0 is the same zero-probability branch from the other side (#126, `bug`):** the sum counts it as landing and by DECISIONS/0024's own reading it contributes nothing; whether a starter board reaches it is unmeasured.
- **The hit A/B stands under every veto variant.** Saltmarsh Ford 171, 170, 168 under two rolls against 126, 126, 122 under one; Old Mill Road's gap 9, 20, 22 points, same direction.

### Twelfth round (2026-09-19, Chat withdraws the weighting; the sum prices a group the move wakes at zero; issue 128, #125 amended)

Chat withdrew survival weighting on Code's measurement, then read `Exposure.Of` for other places the sum counts a branch by possibility rather than by the board that will exist, and found one on all three maps running the other way. Code reproduced it on main with a scratch probe (never pushed).

- **Survival weighting is withdrawn (Chat, agreed).** An expectation read against a hard HP passes boards where the death is a coin flip. The no-crit sum stays a pure worst case for the target's two terms; #117 stands as the narrowing; #125 and #126 agreed as filed.
- **The sum prices a sleeping Guard group that the move itself wakes at zero (#128, `bug`, Chat's finding, reproduced to the game).** `Exposure.Of` reads `EffectiveBehavior` on a board that never ran the wake check, so a group the destination wakes is counted as Hold from where it stands. Old Mill Road, gate 4's captain-alone arm, 200 seeds, two rolls: 13 wins, 167 captain deaths, 20 timeouts, every death dealt by `soldier-1` of the sleeping `mill` group with a `GroupWoke` before it. Gate 1 does not move on any map or scheme; Old Mill Road's Wren row goes 0.855 to 0.630 and joins Pell's in issue 13's body as a number not to author against until #128 lands. The rule: section 8's three wake causes, read through the one predicate `Resolver` uses, lifted so the veto and the rules cannot drift.
- **The death cause is built in the first pass, not deferred (Code's lean; Chat left it open).** With the predicate shared and fed the board after the plan (mover moved, a certainly killed target removed, the fight's two tiles noisy), death costs no line of its own and leaving it out would be a special case. It cannot fire alone on any current map: no weapon reaches past 2 and no group spreads past 5 (`fort`), so the fight's noise at radius 6 wakes any group whose member the captain kills; a probe over 3,261 captain attacks in the baselines found no attack on a sleeping Guard at all, since a move into range wakes by proximity before the attack is issued. It is the rule that stays true once a map spreads a group.
- **One rule, written once.** #117, #126, and #128 are one sentence: the sum counts the board the enemy phase will find, never the board as it stands. It goes into `Exposure.Of`'s summary and DESIGN section 11's gate 1 line, the veto's home, with section 8 naming the shared wake predicate.
- **#125 amended (Chat's two additions, agreed).** Gate 4's rows print the benched arm's losses by cause, #114's breakdown, so `drop 0.855` beside `benched losses 167 captain` cannot be read as a recruit mattering; and every median prints its count, so one timeout reads as one game and not a finding.
- **Known and not in #128:** a recruit's later command this turn can wake a group the captain's sum priced asleep. That is the heuristic player's ordering, not the veto's guarantee; read off gate 4's new loss columns after #128 before anything is filed.

### Engine and renderers (2026-09-14, both partners; DECISIONS/0008)

- The shipping front end is Godot 4 .NET, living in this repo; Unity is out and Unreal is not the main line. The deciding reason is review: Godot scenes are text, so both partners can read every front-end diff. Ironwake.Core stays engine-free through Phase 3 and after. Any renderer consumes the versioned state and event protocol (#25) and carries no rules; reachable tiles, valid targets, and forecasts are queries answered by the core. The Godot project references Core directly and consumes the same event records in-process; the JSON line protocol is for out-of-process engines and bug reports, both paths must agree, and the forecast honesty gate runs over the protocol too. Issue 11 built the CLI as if the protocol existed. An Unreal front end is welcome as Lotus's own second consumer of the protocol, a side line from his desktop; it becomes the shipped one by his ruling, not by a rewrite.

### Plumbing (not design)

- Repo is public; Chat clones, builds, and plays directly. Play by proxy is the fallback (DECISIONS/0003, 0006). Chat has two bodies, the claude.ai Project and a comment-woken routine; both sign "— Chat" (DECISIONS/0007). Code runs on Fable in every body, Chat and the Critic on Opus 5 (DECISIONS/0009).
- Eyes reaction means "read, answer coming." A reply links the comment it answers in its first line.
- **Fun Gate entries are written cold.** Each partner plays a map and writes its PLAYTEST entry before reading the other's. Code keeps its best turn off the Table until Chat's entry is posted; the entry in the repo is Chat's to leave unopened until then. Two 7s are not independent if one partner read the other's best turn first.

## Leaning, not agreed

- **Rivalry's numbers.** As written, +10 crit at triple damage is a formation bonus the player will farm. Three arms, all data: as written; symmetric cost (crit avoid -10 while rival-adjacent, Code's lean); counter-only bonus (the fallback if exposure comes back low). The spike's questions are whether players cluster rivals on purpose and whether the rival-adjacent unit is then attacked.
- **Battalions.** Both partners lean drop; neither has played the replacement. The fork is filed by the decision PR of whichever Cha spike runs first, issue 16 by the queue; #74 is `blocked` until it is ruled.
- **The wake tax floor.** Somewhere between 6 percent (a diamond corner clipping a reach set) and 44 percent (half of everything you can do wakes the group). Set from issue 13's journals, like every other threshold in section 11.
- **Open for Lotus, nothing waiting on either:** one fact (does he want to learn Unreal on this project with Code building through the bridge and him present?) and plumbing from the sixth round (the Builder's stored prompt should carry ROUTINES.md section 2's lines protecting step 3 and the decision record at the new pace; the Critic's schedule should follow the merge rate, nightly at 06:00 after the last Builder run, thinned per ROUTINES.md if the run cap bites).
