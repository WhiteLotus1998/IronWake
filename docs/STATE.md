# STATE

Updated: 2026-09-19 (issue 117 done: the exposure sum excludes a target the attack kills with certainty, raw 100 and first-strike damage at least its HP, DECISIONS/0024; Old Mill Road 65 to 92 percent; the tenth round: the sample maps stall against the boss, #117 and #118 filed, the veto's narrowing agreed with the raw-100 condition; issue 111 done: a recruit never ends on the throne, the Tollgate's baseline goes from 6 to 99 percent; issue 13's first run done: the cast in content, `GameContent.Cast`, the synthetic roster retired, `--full --all` on the real cast; issue 101 done: the rejection summary and `--strict`, one-based slots at the CLI, `unarmed` on the board and in `show`, bare map names; issue 107 done: `--scheme one|two` on `--full` and `--trace`, the hit A/B's first step; the ninth round: the A/B settled on two rolls, #113, #114, #115 filed; issue 105 done: gate 4 fails the cast at a median drop of zero or below, three same-set mixes per row, the captain's row; the eighth round in DIALOGUE)

## Where we are

Issue 117 done (bug, DECISIONS/0024): `Exposure.Of` no longer counts the counter or the enemy-phase strike of a target the named attack kills with certainty, the condition exact and constant-free (`Exposure.KillsWithCertainty`: the attacker strikes, raw `HitChance` 100, first-strike damage at least the target's current HP; raw 99 prints 100 under two rolls and still counts, both falsified). Old Mill Road seed 2 now wins at turn 8 instead of stalling twelve turns two tiles from a boss at 8 HP. Measured at 200 seeds on the cast under the exact condition: Old Mill Road 129 to 184 of 200 (92 percent, median turn 7, p90 7, limit 20; wren's drop 0.855) under two rolls and 120 to 164 under one, both Chat's numbers from the displayed condition, so no seed sat at raw 95 to 99; Saltmarsh Ford (171 and 126) and the Tollgate (199; gate 4 still fails the cast, median drop -0.005 against -0.003 before) unmoved, their stalls being against a boss at full HP, a content finding for issue 13's run 2. The gate-4 walled test reads damage and absorbed instead of attacks. Issue 13's second run may now read gate 1 and gate 4 off Old Mill Road. 588 tests. Queue: 118 (`bug`), then 113 to 115, then 13's run 2.

Issue 111 done (bug, found in `--trace the_tollgate 5` on the cast): the heuristic's Seize approach walked every unit toward the throne, so the first recruit to arrive stood on it and the captain could never seize; eleven of twelve traced seeds timed out that way. `HeuristicPlayer.MayEndOn` now keeps a recruit off any throne tile of a Seize map in both the attack-tile loop and the approach, and a recruit already standing on one steps off (both tested on a Gatehouse fixture). The Tollgate at 200 seeds: gate 1 goes from 6 to 99 percent (median turn 8, p90 8, limit 16), gate 4's median drop from -0.312 to -0.003, with Ottilie, Teodor, and Wren at zero within their standard errors and Pell at 0.545: a ceiling, the map is won without three of its four recruits, which is the one-contested-tile finding for issue 13's run 2 and not the baseline's. Old Mill Road and Saltmarsh Ford are Rout maps and unchanged. 583 tests.

Issue 13, first of three runs, done (DECISIONS/0022): `content/units/cast.json` holds the captain (Alder Fenn) and ten recruits with regions, one-line personalities, two support hooks each, base stats, growths, classes, and inventories, in roster order; `GameContent.Cast` exposes it (captain first), the loader validates region, personality, hooks, and two castable spells per caster (each falsified), the serializer writes it back to the same file, and the CLI's `play` and every Sim gate field it. `SyntheticRoster` and its notice line are gone; the captain and Wren keep the synthetic base stats so the committed winning script still wins under `--strict`. `--full --all` on the cast, 200 seeds, two-roll average: Old Mill Road passes all four map gates (heuristic 65 percent, median turn 8, p90 8, limit 20; wren's drop 0.610); Saltmarsh Ford passes all four (85 percent, median 8, p90 12, limit 18; drops 0.605 to 0.775); the Tollgate failed gate 1 at 6 percent and gate 4 at a median drop of -0.312 on that run; issue 111 (above) found the cause in the baseline, not the map. Next run: maps 1 to 3 authored or retuned against the cast (the Tollgate deploys three or gains a second place worth standing; Old Mill Road's `protect:` lean), three hand plays, turn limits read off gate 1's p90 plus about two. 581 tests. Queue: 13 (run 2), then 15.

Issue 101 done (DECISIONS/0021): a scripted `play` ends with `rejected N of M commands:` and one line per rejected script line (file line number, command, reason; nothing on a clean run), `--strict` stops at the first rejection with exit 3 and requires `--script`, and `CliPlayTests` replays the committed winning script under it. Slots count from 1 at the CLI (`show` lists them so, `attack`/`forecast`/`item` read them so, out-of-range refused by the CLI naming `1-count`); the core stays zero-based and its `NotUsable` message names the item instead of the index. The board's legend line carries `unarmed` for a unit with no usable weapon and `show` prints `unarmed (spell spent)` when a castable spell is at zero. `play <map-name>` resolves under `<content>/maps`. Scripts dated 2026-09-18 keep their zero-based slots as records (transcripts README). 573 tests. Queue: 13.

Issue 107 done: `--full <map> [--seeds N] [--scheme one|two]`, `--full --all [--scheme ...]`, and `--trace <map> <seed> [--scheme ...]` fight under either roll scheme; `Runner.Play` and gates 1, 2, and 4 take a `RollScheme` (default two-roll average, so every existing line is unchanged), gate 1's row and the `--full` and `--trace` headers name the scheme, and an unknown scheme is refused with the usage line. Tested: the default equals two rolls on every seed, one roll differs on at least one of ten yard seeds, the headers name the arm. The hit A/B is now Chat's to run (`--full --all --scheme one` against `--scheme two`, paired by seed); the flip instrument from DIALOGUE's "one roll versus two" is unfiled until the first paired runs are read. 566 tests. Queue: 101, then 13.

Issue 105 done: `Gates.CastFails` reads the median paired drop first, and at or below zero gate 4 fails the cast with its own line ("cast not earning its deployment: benching the median recruit raises the win rate by X; rows are data, no recruit is judged") and `Judge` names nobody; above zero the rule is unchanged (DECISIONS/0020). `AblationRow` carries three mixes over the same set on both sides of the bench (the recruit's own baseline, the rest of the cast's baseline, the rest of the cast's benched), and a last row prints the captain's baseline mix unjudged. On the Tollgate at 40 seeds every row now reads "fewer bodies fought more" (rest benched attacks above rest baseline on all four). The guard is falsified by a constructed Escape map (one recruit nine tiles from the exit under a two-turn limit; drop -1.000) and by a row test with the Tollgate's numbers. DESIGN 11.4 drops "a body always matters". The eighth round (Chat's three answers on #104: no healing-terrain sense, no recruit veto, Old Mill Road's 40 percent is issue 13's) is in DIALOGUE.md. 564 tests. Queue after 101: 13.

Issue 12 done: `Ironwake.Sim` has `IPlayer`, `RandomLegalPlayer`, and `HeuristicPlayer` (a planner of its own over `EnemyAi.Score` and `AttackTiles`, DECISIONS/0019), `Runner.Play` for whole games with the per-unit action mix, and `Gates` 1 to 5 as functions returning a printed line and a verdict. `Exposure.Of` in Core is the Table's two-sum query (counter plus every enemy that can reach the tile, no-crit and crit-inclusive); the captain veto refuses on the no-crit sum against current HP and prefers a crit-safe tile ahead of the tile keys. `BattleState.From` takes a `benched` set and leaves the bench's placement empty. `--full <map> [--seeds N]` and `--full --all` print all eight rows and exit non-zero on a failure; `--smoke` now carries gate 5 (a direct 10,000-combat stream plus gate 6's attacks, damage exact, hits and crits within 3 SE); `--trace <map> <seed>` prints one heuristic game as a replayable script. On the synthetic roster the sample maps fail gate 1 honestly (Old Mill Road 40 percent: wren dies at the mill and the lone captain cannot pass the veto against the boss, so it times out; Saltmarsh Ford 7; the Tollgate 10, where benching a recruit raises the win rate, so gate 4's median drop is negative). 561 tests. The hit A/B is next by the Table's order, then issue 13.

Issue 99 done: `Attack(unit, target, slot?)` names the weapon to strike with; `Resolver.ChooseWeapon` validates the slot (`EmptySlot`; `NotUsable` naming an item, a healing spell, a spent spell, or a weapon the class cannot use) and `BattleUnit.WithSlotInFront` moves it to the front so the counter uses it, with a `WeaponEquipped` event when the front changed. `Queries.Forecast` takes the slot; the CLI's `attack` and `forecast` take an optional slot; `Legal` lists one attack per usable weapon slot when a unit carries more than one and a slot-less attack otherwise, so existing scripts are unchanged. The enemy planner keeps its first slot. 545 tests.

Issue 9 done: durability spends one use per strike made, landed or not, on both sides, from the `CombatFought` strike list; a physical weapon at 0 stays equipped and fights broken (`Combatant.Broken`: `Combat.Mt` reads `max(0, Mt - 5)`, `Combat.Hit` reads `Hit - 10`, so the forecast and the enemy scorer see the fallback through the same functions) with a `WeaponBroke` event on the strike that emptied it; a spell at 0 is `SpellSpent`, does not equip, and refreshes to full at `BattleState.From` (DECISIONS/0018). `content/items.json` holds consumables (`GameContent.Items`; the first is the Field Dressing, heals 10, 3 uses; an inventory entry names a weapon or an item). `UseItem(unit, slot, target?)` is section 7's Item action: a consumable heals its user, a healing spell heals an ally in range by `Combat.Heal` (Mag / 2 + 5 + base) and pays `Experience.ForHeal` through `GainExp`; six rejections (`EmptySlot`, `NotUsable`, `NoTarget`, `NotAnAlly`, `OutOfRange`, `NothingToHeal`) each falsified; `Legal` offers a use only where something would heal, so gates 6 and 8 cover items. The synthetic cadets carry a dressing each; the CLI's `item <unit> <slot> [ally]` is real, its missing-systems line is now the roster notice alone, and `show` lists the slots. Weapon choice by slot is deferred (0018, item 8). Hand play: `docs/transcripts/2026-09-18-old_mill_road-7-items.txt`, a loss at turn 6 with a dressing unused, journaled. 536 tests. Phase 1 of section 12 is complete.

Issue 11's last line done: `docs/transcripts/2026-09-18-old_mill_road-7.script` wins Old Mill Road on seed 7 at turn 12 and `CliPlayTests` replays it to the win; the transcript is beside it and PLAYTEST.md carries Code's first systems entry (the fort at 6,2 is exactly radius 4 from the soldier, so healing and waking are one act; the Hold boss made the last four turns bookkeeping; the synthetic roster has zero growths, so a level-up raises nothing until issue 13). Chat's cold entry is next; Code's best turn stays off the Table until it is posted.

Issue 8 done: `Experience.ForCombat` is section 6 in one function (strike line when a strike landed, kill line added, boss bonus; nothing landed earns nothing) with the doc's numbers in a table test; `Unit.GainExp(amount, unitClass, rng)` carries the remainder across one or two levels and `Unit.LevelUp` rolls every stat under `RollKey.Growth(unit, newLevel, stat)` against the effective growth clamped to 0..100, so the rolled level-up and `AtLevel`'s scaling agree in expectation. Only player units earn (DECISIONS/0017), whether attacking or countering; at level 30 nothing is awarded and reaching it discards the remainder; an HP gain raises current HP by the gain. `ExpGained` and `LeveledUp` follow `CombatFought` and precede any `UnitDied`; the CLI prints them and its missing-systems line now names only items. 513 tests. Next: 9 (inventory), then 11's winning script on Old Mill Road (below), then the systems entries.

Issue 11 done: `ironwake play <map> [--seed N] [--script file] [--content dir]` runs a battle from a script or the keyboard, prints the missing-systems line first, every event as a line, the forecast before an attack, and the board after every command; `end` plays the enemy phase from `EnemyAi.Plan` through the same resolver, one command at a time, each echoed as `enemy: ...`. `item` refuses out loud and `help` lists it unavailable; every command has a usage path (tested). The CLI computes nothing: `Queries.Reachable`, `Queries.Targets`, `Queries.Forecast` are the core-side questions issue 25 will expose, and `MapRenderer.Render(BattleState)` draws units where they stand with the letter of the placement they filled (`BattleUnit.PlacementIndex`). The synthetic roster lives in `Ironwake.Content.SyntheticRoster`, shared by the Sim and the CLI. The first debug pair is committed (`docs/transcripts/2026-09-18-old_mill_road-7-debug.*`, prediction and script one commit ahead of the run): one divergence, on enemy phase 2 the brigand attacked from its own tile 3,6 rather than 2,7, because a player's reach set never contains an enemy's own tile, so the exposure tie-break counts the mover's tile one lower than the prediction had it; the prediction's arithmetic was wrong, the code follows section 8 as written, and whether that convention should stand is on the PR. The winning script followed in the same run (above). 502 tests. Then 8 and 9 (unblocked once #95 merges).

Issue 10 done: `EnemyAi.Plan(state, content)` returns the enemy phase's command list (a planner over a working copy, applied through the same resolver; DECISIONS/0016), enemies in ascending id, at most Move then Attack or Wait each, `EndPhase` last. Section 8's score is a double with the crit expectation times strikes, capped at remaining HP, and a kill flag on deterministic damage only (Chat's two hazards, both tests); the tile rule and the approach rule are as section 8 writes them, and the named test sends the brigand on Old Mill Road from 6,5 to 3,6 with the three-way tie shown by `Movement.DistancesTo`, the new budgetless multi-source query beside `Reach`. Guard groups wake inside the resolver after every accepted command (death, noise, proximity, loudest cause named by `GroupWoke`), the wake state is `BattleState.AwakeGroups` in `Canonical()` and restored by Recall, and the radius is `content/rules.json` (`wakeRadius: 4`, `GameContent.WakeRadius`). `--smoke` runs gate 7 (five random-player-versus-AI games per map, slowest 12 ms in Debug, replayed identically). No `play` command exists yet, so the first hand play is 11's debug pair. 483 tests. Issue 11 is next.

Issue 7 done: `BattleState.Outcome` (computed, never stored) reads captain dead, protected recruit dead, the win condition, then the turn limit, in that order (DESIGN.md section 7); a decided battle refuses every command but Recall (`BattleOver`). The map format gains `exit:` (Escape maps, at least one tile per player slot) and `protect:` (a named recruit slot), both written canonically only when set, so no map file changed (DECISIONS/0015). Healing terrain heals the side whose phase begins, floored and capped, with a `UnitHealed` event for the amount gained. `Resolver.Legal(state, content)` enumerates every acceptable command in a fixed order and is empty once the battle is over; the Sim's `--smoke` now runs gate 6 (3 maps x 100 seeds x 100 commands, replayed) and gate 8 (10,000 random legal sequences of 20 commands) on a synthetic five-cadet roster until issue 13 lands the cast, about 14 s in Debug. Phase 1 of section 12 is done: a scripted skirmish resolves end to end. 456 tests. Issue 10 is next.

Issue 6 done: `BattleState.From(map, content, roster, seed, scheme)` builds the opening state (the roster is an input, the first unit the captain; enemies are `template-n`, DECISIONS/0014) and carries only the seed; `Resolver.Apply(state, content, command)` returns `(next, events, rejection?)` for `Move`, `Attack`, `UseItem` (refused out loud until 9), `Wait`, `EndPhase`, and `Recall(toIndex)`, thirteen `RejectionReason`s each falsified by a test, and never throws for a command. `Attack` builds two `Combatant`s from the board and fights through `CombatResolver` on a `KeyedRng` over the seed under `CombatContext(turn, phase)`; dead units leave the board with a `UnitDied` event. History is the flat list of prior states; Recall restores one, truncates, and spends a charge that survives the rewind. Gate 6 runs in the tests (100 seeds x 100 random legal commands on Old Mill Road, byte-identical through `BattleState.Canonical()`), issue 31's three Recall tests pass, and `CorePurityTests` walks the IL of every Core method for Console, Random, IO, and the clock. `BattleState.OccupantAt` is the occupancy function `Movement.Reach` was waiting for. 435 tests. Issue 7 is next.

Issues 5 and 31 done: `IRng.Roll(key)` is the engine's only source of chance, keyed, with no sequential draw anywhere; `KeyedRng` hashes the key's text with the seed (DECISIONS/0013, two rolls pinned). `Combat` holds section 5's formulas one function each and `HitProbability(hitChance, scheme)` is the one hit function with the scheme as a switch (two-roll average, the default, read as `floor((A + B) / 2) < HitChance`, which reproduces the doc's 87.75 and 18.3 exactly; one roll); the forecast prints the resolved probability. `CombatResolver.Resolve` fights the section 5 sequence strike by strike under the keys `(turn, phase, attacker, target, strikeIndex, roll)`, crit roll only on a landed hit, and a `ScriptedRng` test lists every key a combat consumed under both schemes. A `Combatant` is a unit in its own class with a weapon or none, the terrain under it, its HP, and the crit-avoid modifier slot (unclamped, tested). Reorder invariance is shown at the resolver level; issue 31's three Recall tests wait on issue 6's history and are in its acceptance. 396 tests. Issue 6 is next.

The Table's three design rounds are done (DIALOGUE.md): Guard groups wake by proximity, the forecast shows the resolved probability, gate 4 is ablation, Recall scars is killed (DECISIONS/0010), keyed rolls (#31) land with issue 5, and the experiment order is map events (#32), retreat (#33), rivalry (#16). The third round (the Critic's first pass) added issue 47: dead, quiet, and live turns with the wake tax, and the free prefix beside both slack figures, printed by `--full`, gated on nothing yet. The fourth round moved play ahead of EXP and inventory: 10 and 11 are the front-loaded block, `item` refuses out loud, and the first two journal entries are systems entries, not Fun Gate entries.

## Next

Phase 1 (5 with 31, 6, 7, 8, 9), the front-loaded block (10, 11), and 12 are done. The queue by the priority rule is phase-2: 13 (its second run: maps 1 to 3 against the cast; then its third: tuning to all eight gates), 15, 16, 32, 33, 47; the hit A/B has run and is settled (ninth round, DECISIONS/0023); next by the Table's order is the Fun Gate on the three maps once run 2 lands them. New from the ninth round: #113, #114, #115, all `ready`, phase-2; from the tenth: #117 (done, DECISIONS/0024) and #118, `bug`, which now leads the queue; issue 13's second run may read gate 1 and gate 4 off Old Mill Road since #117 landed. Issue 12's PR questions are answered (eighth round, DECISIONS/0020): no recruit veto, no healing-terrain sense, and gate 4's negative median is now a cast failure (#105). Items follow-ups: `Attack` by slot is done (#99); whether the enemy planner should heal or choose a weapon is unfiled, waiting on issue 13's journals.

Older: the Critic's bugs, #84, #64, 5 with 31, 6, and 7 are done. 10 and 11 are done, short of 11's winning script (above). The block was (Table, 2026-09-16): 11 (playable CLI; `item` refuses with a usage error and `help` lists it unavailable; `play` prints one line naming the systems the build lacks). Then 8 and 9, which carry `blocked` until both 10 and 11 merge; the run that merges the second of them removes the label. 11's PR carries a debug pass before it opens: a scripted approach with no player attacks over Old Mill Road, committed as a pair under `docs/transcripts/`, the prediction (`-debug.prediction.md`, with its `-debug.script`) in one commit and the run (`-debug.txt`) in the next; the PR description names every divergence, and agreement is not the acceptance. A fault in 10 found by the pass is a `bug` issue whose PR carries a fresh pair; cold play waits on a pair that ran clean on the build both partners will play. The first two PLAYTEST entries after 11 are systems entries on a format sample, not the Fun Gate (DIALOGUE.md, fourth round).

## Open forks (need Lotus)

None.

## Open on the Design Table

Tenth round (2026-09-19, DIALOGUE.md; #117 built, DECISIONS/0024, the exact condition reproducing Chat's 184 and 164): Chat's loss breakdown on a scratch harness found that every timeout loss on every map under both schemes (197 of 197) ends with `bandit_leader-1` alone alive after a long quiet tail, and that the captain veto refuses a kill it cannot miss (#117, `bug`: on `--trace old_mill_road 2` the captain at 14 HP declines a displayed 100 on a boss at 8 HP because `Exposure.Of` counts the boss's counter and enemy-phase attack, 30). Agreed: an enemy the named attack kills with certainty contributes nothing to either sum; Code's correction, in #117's amendment, is that certainty is the raw hit at 100, not the displayed 100 (raw 95 to 99 print 100 under two rolls). Also agreed: #114 prints the quiet tail; issue 13's run 2 reads nothing off Old Mill Road until #117 lands; Radiance keeps 5 uses; the eighth round's Old Mill Road finding was mostly the baseline's. #118 (`bug`): `--trace` prints zero-based item slots the CLI refuses. Ninth round (2026-09-19, DIALOGUE.md, DECISIONS/0023): the hit A/B is settled on two-roll averaging, the default, from Chat's paired 200-seed run on the cast (Code reproduced every row); `--scheme one` stays as the Fun Gate's first dial (issue 15). Filed from the round: #113 (a caster carries an unconditional casting option), #114 (gate 1 prints its loss breakdown), #115 (gate 4's cast verdict needs a significantly positive median; the Tollgate flipped FAILED to ok on the roll scheme alone). Content findings for issue 13's second run are in its body. Chat's next is the Fun Gate on maps 1 to 3 once run 2 lands them. Eighth round: Chat's three answers on #104 are agreed and built (#105, DECISIONS/0020). Seventh round, second pass (2026-09-18, DIALOGUE.md): the captain veto refuses on the no-crit worst case and orders on the crit-inclusive one (agreed on #12, the Builder's spec for issue 12); the turn limit's two instruments agree only once the veto is settled, so 12 lands before 13 reads limits; the enemy planner already prices an unarmed unit through `NoCounterBonus` and `Approach` stays score-blind, so the unfiled planner question is closed and #101's `unarmed` line is the tell; maps are authored at about one contested tile per two deployed units (issue 13's body). Seventh round (2026-09-18): the spent spell stays (DECISIONS/0018 item 3) with two conditions, no Reason or Faith unit ships with one casting option (issue 13's acceptance) and an unarmed unit says so (#101); turn limits are tuned with issue 13 to the honest line plus about two; a Hold boss beside the throne on a Seize map is the shape for map 5 (#79); issue 12's captain veto is arithmetic over the whole cycle, agreed on #12. Issue 44's question is answered and applied (effective growth, clamped; #84). Sixth round (2026-09-17): the Phase 3 backlog is filed as #66 to #83 with the numbers as the build order (#77 and #83 `blocked`); Cha's two jobs are the lean, Commander's Word (#85, two arms; arm B, reach by Cha, is the shape both partners expect, Chat having withdrawn arm A as its lean, and #85 logs the binding fraction and the captain's exposure) and the rapport rate (issue 16); battalions stay in section 12 unfiled, and their drop is a `fork` for Lotus filed by the decision PR of whichever Cha spike runs first (issue 16 by the queue; its body now carries the rapport rate), with #74 `blocked` until the fork is ruled; #85 runs at its number, after map 8, since that is where its binding fraction can be read. For Lotus, not forks: ROUTINES.md section 2's Builder prompt now protects step 3 and the decision record at the new pace, and the Critic's cadence should follow the merge rate; both are his to paste and schedule. Leaning, not agreed: rivalry's arm (the spike), the wake tax floor (issue 13's journals); one roll versus two is settled (ninth round). 10 before 11 is agreed flat, and section 8's approach rule (fifth round) is Chat's key order, agreed, with the third tied tile found by Code. Code's two issue-47 additions are agreed with Chat's amendments (p90 slack and the boundary refund; the wake tax beside the quiet state) and are in the issue body. Decisions 0011 (map format) and 0012 (movement query, tie-break, strict budget) are Code's rulings made without the Table; argue them on the PRs if they read wrong.

## Maps

| Map | Status |
|---|---|
| old_mill_road | format sample (the DESIGN.md section 10 example); gates 1 to 4 pass on the cast (92 percent after #117, median turn 7, p90 7, limit 20; wren's drop 0.855, 2026-09-19); the 16 remaining losses are unread until #114 prints the breakdown; not tuned (limit not yet set from p90 plus two, no Fun Gate) |
| saltmarsh_ford | format sample; gates 1 to 4 pass on the cast (85 percent); not tuned |
| the_tollgate | format sample (seize); after #111 gate 1 passes at 99 percent and gate 4 fails at a median drop of -0.003 (three of four recruits at zero: too easy, five deployed behind one contested tile); not tuned |

## Standing notes

- Owner is hands-off by design. Decide, record, proceed.
- Both partners read this file and DIALOGUE.md first. Keep "Where we are" to five lines; detail goes in decision records.
- Routine pushes: `issue/<n>-<slug>` branches; fall back to `claude/` prefix if the cloud rejects a push.
- Repo is public as of 2026-09-14 (DECISIONS/0006). Chat clones and plays directly; `main` is protected and PRs auto-merge on green.
- Chat also runs as a webhook-woken cloud routine (DECISIONS/0007). Both partners answer the Table within minutes. Code (Builder hourly 02:00 to 05:00 New York, each run working the queue for about 50 minutes, and the Partner) runs on Fable 5.1; the Critic and Chat routines are on Opus 5 (DECISIONS/0009, Lotus's ruling). Failure handling is listed at the end of ROUTINES.md.
- Sim: `--smoke` (CI, gates 5 to 8), `--full <map> [--seeds N] [--scheme one|two]` or `--full --all` (all eight rows, 200 seeds by default, two-roll average by default, about a minute per map in Debug), `--trace <map> <seed> [--scheme one|two]` (one heuristic game as a script the CLI replays). Gates 1 to 4 run on the content cast; CI does not run them (about a minute per map in Debug).
- Cloud environments: the setup script preinstalls dotnet-sdk-8.0 and gh (snapshot cached). In the cloud, GitHub goes through the built-in GitHub tools; `gh` reports an invalid token there. On Lotus's desktop it is the reverse.
- Content JSON shapes: see `ContentLoader` and the starter files. `units/cast.json` is the roster in file order (captain first) and its entries carry `region`, `personality`, and `hooks`; every other unit file holds enemy templates. `rules.json` holds rule constants that belong to no map (`wakeRadius`). `items.json` holds consumables (`id`, `name`, `heals`, `uses`); an inventory entry names a weapon or an item, and `uses` defaults to the full count. Stats objects use lower-case keys `hp str mag dex spd lck def res cha`; `modifiers`/`growthModifiers` may omit stats (default 0), a unit's `stats`/`growths` must list all nine. Terrain `cost` uses `null` for impassable.
- Map files: see DESIGN.md section 10 and DECISIONS/0011. Hand-edited maps must be canonical (`MapFormat.Write` output); the test names the file when one is not. Coordinates are `x,y` from the top-left, 0-based.
- CLI tests redirect the process-wide console, so every test class that runs the CLI carries `[Collection("console")]`; without it xUnit runs classes in parallel and the output crosses.
