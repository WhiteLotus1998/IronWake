# STATE

Updated: 2026-09-26. Rewritten, not appended: this file is what is true now, not what happened.

## Where we are

Phase 1 of DESIGN section 12 is complete and Phase 2 is most of the way: rules, content loading, maps, movement, combat on keyed rolls, battle state with history and Recall, the turn loop, EXP, items, the enemy AI, the playable CLI, and the Sim harness with gates 1 to 8. The cast of eleven is in content. `ironwake play <map> [--seed N] [--script f] [--strict]` plays a real battle and both partners have played by hand, and its forecast answers from any tile a unit can still move to (#151), so a tile is chosen with its numbers in view, and a healing tile says what it heals wherever the console names it, `Fort (heals 20 percent, 3 hp)` in a unit's row and `show`, the percent in the map legend, from the function the resolver applies (#207); and `threat <unit> [from <x,y>]` lists every enemy that could strike a unit on the coming enemy phase with the weapon, tile and forecast the planner would use on it, then the total if all land, one enemy per strike tile so two that share the only way in count once (#217, #253), so the door on the Tollgate is priced before it is entered, an enemy an announced event brings that phase priced and marked `(arrives this enemy phase at x,y)` while an unannounced one stays unpriced (0045), and every sleeping group that could strike the tile if woken named with its members and the wake rule, no numbers (#248); and `play <map> --protocol` plays the same battle over JSON lines, every event, command, query answer and state in the shapes of `docs/PROTOCOL.md` (protocol version 1, #25, DECISIONS/0046), the full state reading back equal with its history and gate 5 counting its forecasts through the protocol; Phase 3 has begun: abilities are content (#66), `abilities.json` entries with one effect from a closed set, a passive stat delta or an on-combat hit, avoid, crit and crit-avoid modifier keyed on the opponent's weapon or movement, applied in `Combatant.Stats` and the hit and crit chances so the forecast, resolver and both planners price them without knowing them, gate 5 drawing both kinds on both sides; the file ships empty and a class's `mastery` names an ability without granting it; weapon skill ranks are in (#67, DESIGN section 6): points per weapon type, 3 a combat and 5 a kill once per combat and 3 in Faith a heal, E to S at 0/30/80/160/280/450, a weapon's required `rank` in `weapons.json`, equipping refused below it by name, `rankRaised` on a threshold and `ranks:` in `show`; every shipped weapon needs E, so nothing is gated yet and no game changes; combat arts are in (#68, DESIGN section 5): an ability of kind `art` declared as `attack <unit> <target> [slot] [art <id>]`, the weapon striking with the art's deltas through the unchanged formulas so a Wt delta costs a double, the extra cost paid hit or miss, refused unknown, off-type, under rank, broken or unable to pay, never on a counter, gate 5 drawing an art for half its attackers; no shipped unit knows an art, so no game changes; Breakers and class mastery are in (#69): `abilities.json` ships the seven Breakers (+20 hit and avoid against one weapon type, on strike and counter; Fistbreaker since #70), and a class naming a `mastery` names `masteryPoints`, a player unit earning one per combat fought in the class and keeping the ability after a class change (`masteryEarned`, `masteryPoints` in the protocol, a `mastery:` line in `show`); every starter class masters one ability at 12 combats, the chaplain at 8 (DECISIONS/0047: Fundamentals, Horsebane, Bloodrush, Deadeye, Deep Study, Grace, and Lancebreaker, Bowbreaker and Reasonbreaker for the outrider, skyrider and bulwark), a map and a half for a front-liner, so a single map rarely shows one until #74 carries points between maps; gauntlets are the seventh weapon type (#70): an attack or counter with one is a round of two strikes, each rolling on its own key, so a doubling gauntlet lands four, and it spends one use per combat; the forecast, section 8's scorer, the exposure sum, `threat`, the Sim's kill chance and the console (`x4`) all count `StrikeCount`, the protocol side carries `strikesPerRound`, and gate 5 fights drawn gauntlets and checks every combat's strike count; their content is in (DECISIONS/0048): Iron Gauntlets (Mt 2, Hit 85, Wt 2) and Steel Gauntlets (Mt 4, Hit 80, Wt 5), no crit, the reaver wields them and Keziah carries a pair beside her axe, and Fistbreaker ships as the seventh Breaker; Keziah is on no shipped map, so no shipped game changes, and Code's hand play on `docs/samples/old_mill_road_keziah.map` (seed 7) never swung the axe after turn 2 on that map's soft targets; Canto is in (#71): an ability of kind `canto` that the outrider class carries through a class `abilities` list (held while in the class), so Ansgar has it and so does the Tollgate's rider; after an Attack, Item or Wait the unit is owed its Mov minus the path cost its Move spent (its full Mov if it did not move), taken with `canto <unit> <x,y|stay>` any time before its phase ends, its own tile a legal Canto, the wake check run on where it ends, `threat <unit> from` answering over the Canto's reach, `canto` in the protocol's unit, command and event (`cantoed`), and gate 6 drawing Cantos; the enemy AI never takes one; no shipped map deploys Ansgar, so no shipped game changes, and `docs/samples/canto_raid.map` is its hand-played sample; certification as a one-turn puzzle is spiked (#73, experiment 13.6, DECISIONS/0049): a `certification: <class> <item>...` header makes a one-slot map a trial whose candidate (`play --candidate <id>`, the captain by default) plays in that class with that loadout, the console printing the map's events and whether the class was earned, nothing granted until #74; the two trials now ship under `content/trials/` and stand in for the seal on the campaign screen (#252, DECISIONS/0057), the Outrider's rebuilt with a guard, and #73 closes on Chat's cold play of the rebuild; class certification is in as a Core rule (#72): a class's optional `certification` block in `classes.json` (level, weapon ranks, stat minimums against the unit's own stats), `Certifications.Check` listing every failed requirement (`needs lance D, has E`) and `Certify` changing the class and nothing else, mastery kept, no roll; no shipped class asks anything and nothing calls it outside tests until the between-map screen (#74) spends a seal on it; the Recall browser is in (#75, DESIGN section 7): `recall list` prints every state a Recall can return to with its turn, the command that made it and what a rewind there undoes (kills, EXP, levels and enemy HP given back; units, HP and unarrived spawns returned) from `RecallCost.Of`, `recall n` prints the same line and that the rolls do not change, and at zero charges the list says so and the rewind is refused; its transcript is `docs/transcripts/2026-09-25-old_mill_road-7-recall.txt`; the protocol carries none of it yet; difficulty is data (#76, DESIGN section 9): a `difficulties` block in `rules.json` with per-stat percents, an enemy level offset and a Recall override, `normal` required to be the identity, applied once by `MapDefinition.Under` and to every enemy and spawn through `EnemyUnit`, a `difficulty:` header only on a map played under one (refused in `content/maps`), and `--full <map> --difficulty <id>` running the gates under it; only `normal` ships, Hard's entry waits on the Table, and the campaign record that selects it is #74's; the between-map screen is in (#74, DECISIONS/0051, numbers provisional): `content/campaign.json` plays the three sample maps in order with a purse, rewards, fixed stock and a seal price, `price` on weapons and items, and `ironwake campaign` runs the screen before every map (buy, repair at price over durability a use, certify through #72 for the seal, bench as a deployment choice, `record` as the protocol's campaign shape) then the battle until `leave`; permadeath carries, a named slot whose recruit fell stays empty, a `supplies` cap's held-back uses come back after the map, each map plays on the campaign seed plus its index, and Code's two-map play is seed 139 (`docs/transcripts/2026-09-25-campaign-139.txt`), where Wren mastered Cadet on map 2 on carried points; the three sample maps pass gates 1 to 4 on the cast but none is `tuned`; Old Mill Road has both Fun Gate entries on DECISIONS/0031 and fails it (Chat 5/6/5 cold on seed 83, Code 7/6/7 on seed 73; twenty-fifth round, #160), Saltmarsh Ford has both and fails it on choice (Code 7/6/7 on seed 13, Chat 6/6/5 on seed 17; eighteenth round, #131), the Tollgate passes it from Chat's chair and fails only on Code's surprise (Code 7/7/6 on seed 131, Chat 8/7/7 cold on seed 151; thirty-second round, #222), so every sample map has both entries and fails, and issue 15's first pass is done. 1245 tests green on Linux and, from #132, on the `ci-windows` job too. The hit A/B is settled on two-roll averaging (DECISIONS/0023). The exposure sum counts the board the command certainly produces: a certain kill removed (#117), a strike at raw hit 0 dropped (#126), and a group the command wakes counted by its move through the wake predicate the resolver shares (#128, DECISIONS/0026). Issue 13's run 2 has started: Old Mill Road is retuned against the cast with its own boss and a limit of 12, all eight gates passing (DECISIONS/0027). The heuristic's veto covers every unit whose death loses the map (#141): the captain and a `protect:` recruit, through one predicate, and the core refuses to bench either. Gate 1 and gate 4 print the refused-kill median over their timeouts, floored at four decimals so only a certain kill prints 1.0000 (#153), and gate 4's rows the benched arm's losses by cause (#125); the number counts the counter between the strikes on every path through the second strike (#147); on the Tollgate the stall board reads 0.72 but Pell's row still prints 1.0000, since the game's highest refusal is a turn-6 gate board, and the captain holds a dressing in all 89 of his stalls, so his row is printed and not trusted (fifteenth round). The veto is frozen after #147 (sixteenth round): further baseline work is filed and waits behind the Fun Gate. Issue 158 is kept (DECISIONS/0028): section 5's burden is weight less the whole of Str, attack speed counts twice in physical avoid, the iron tier's hit is 15 lower in content, and the fort's avoid is 15, so the player's raw hit on open ground sits near 60 to 80 and terrain shows; the arms and `--formula` are retired, `--scheme` stays in both front ends, and `--hitband` stays as a standing number over what ships. On what ships gate 1 reads Old Mill Road 82 percent, Saltmarsh Ford 34 (failing; the rest is the heuristic's stall before the healing boss, #131's), the Tollgate 99, with gate 4 passing on all three and gates 5 to 8 green (`docs/measurements/2026-09-25-full-keep-200seeds.txt`); Code's first play on it is Saltmarsh seed 61, 6/7/6. An enemy's attack options range over every weapon it carries and the winning Attack names the slot (#174), and the exposure sum reads the same weapons; the bandit leader carries the Steel Axe and the enemy-only Toll Axe (1-2, Mt 6, Hit 60, Wt 8): steel beside a fast cadet, the throw at range 2, and he counters with what he last swung (DECISIONS/0029, #131's target 2). On it gate 1 reads Saltmarsh 17 percent with gate 4 failing on the ceiling line and the Tollgate 92 percent with Ottilie's row dead weight, all timeouts with captain deaths flat; Code's partial replay of seed 61 won on turn 12 with the boss declining the bait to throw at Ottilie. Issue 131's target 1 is in too: forest at 3,4 and 4,4 south of the west crossing, so the brigand's approach stops there and the ford group holds the crossing mouth instead of dying on open ground (DECISIONS/0030); gate 1 on Saltmarsh reads 11 percent under two rolls (timeouts, the same stall), and Code's hand play on it is seed 71, 7/7/6. Old Mill Road's slice (#160) is in: the mill bandit is an ordinary guard of the mill and wakes and walks with it (DECISIONS/0031), gate 1 78 percent with 4 timeouts and 41 captain deaths, Wren's drop 0.500, all eight gates passing; Code's hand play is seed 73, 7/6/7, won on turn 7 with one Recall. The play found #190, now fixed: a Recall returns only to a player-phase state, refused otherwise without spending a charge and naming the nearest legal states, and bare `recall` lists each player turn's starting state (DECISIONS/0032). Old Mill Road's opening pass (#160) is in: a road archer at 8,7 trails the brigand so the pair strike together on enemy phase 2 (DECISIONS/0034); Code's hand play is seed 101, 7/7/6, won on turn 10 with two Recalls, and gate 1 falls to 54 percent (55 captain deaths from Wren dying early on the road). Chat's cold re-rate is seed 131, 6/7/5, won on turn 8 with one Recall; 0034 is kept, the map fails on tension and surprise, and the next lever is the dressings, a `supplies: N` map header capping every deployed unit's consumable uses (map 1 at 1), then the party's start, then the archer's numbers; gate 1 is owed after it (twenty-eighth round). The Tollgate's slice (#181) is in: a Toll Warden with an enemy-only 1-2 spear holds the door at 6,2, so no tile outside the keep strikes it unanswered (a test holds it), four deployed without Ottilie, limit 10 (DECISIONS/0033); gate 1 75 percent with 50 stall timeouts and no captain deaths, gate 4 passing at 0.480, all eight gates green; Code's hand play is seed 97, 6/6/7, seized on turn 7 with no Recall. Chat's cold re-rate is seed 101, 7/6/5, seized on turn 10 of 10; 0033 is kept and the woods group is #197. The Tollgate's woods pass (#197) is in: the woods group holds its forest and the brigand at 6,5 is a Toll Brigand with the 1-2 Toll Axe, so the hill is struck while it lives and a strike on it from range 2 is answered (a test holds it; DECISIONS/0035); gate 1 69 percent with 63 timeouts at a quiet tail of 0.3 and no captain deaths, gate 4 passing at 0.330, all eight gates green; Code's hand play is seed 127, seized on turn 9 of 10; Chat's cold re-rate is seed 139, 7/7/6, 0035 kept. The woods archer is at 5,5 in new forest (#208, DECISIONS/0041), so every tile beside it lies in the toll brigand's 1-2 band and it can still be shot from outside that band (a test holds both), and a test pins the boss throwing at Teodor behind the door over swinging at the captain; Chat's cold re-rate on 0041 is seed 151, 8/7/7 (thirty-second round), 0041 kept, forest and the limit of 10 with it, and the map fails the Fun Gate only on Code's surprise of 6, so the rider now arrives by map event, `turn 4 enemy` at 13,5, during the woods fight instead of standing at 12,5 for the same turn-2 kill (#222, DECISIONS/0045); gate 1 80 percent with 41 timeouts and no captain deaths, gate 4 passing at 0.310 (Pell 0.535, Teodor 0.310, Wren 0.270), all eight gates green; Code's hand play is seed 163, 8/7/7, seized on turn 10 of 10 with one Recall, the rider killing Pell on the woods fight's flank before the Recall; `threat` names the rider on player phase 4 before it spawns, an open question in 0045; the map waits on Chat's cold re-rate of this version. Map events are in (#32, DECISIONS/0036). An `events:` block after the units declares `turn N player|enemy` and `enter x,y` triggers with `terrain`, `spawn` (edge tile) and `flag` actions. Each event fires once, a held tile blocks and spends it, and spawned ids are fixed by the file. The sample is `docs/samples/sluice_gate.map`, which stays outside `content/maps` so that no gated map moves; Code's hand play of it is seed 7. Enemy retreat is in as a spike behind a per-map `retreat: on` header (#33, DECISIONS/0037). An Aggressive enemy below 30 percent with a Fort or Throne in reach falls back to it through a `Retreat` command and does not attack, and it never retreats twice. No gated map carries the header. The sample is `docs/samples/old_mill_road_retreat.map`; Code's hand play of it is seed 41, 5/6/5, where the rule fired once and cost nothing; Chat's is the same seed-131 command list as the shipped map, where it fired twice and both times took away the dying bandit's last swing, one of them a 2 percent crit that had lost the shipped game. As built it is a mercy rule. 0037's two amendments are in (#204): the refuge must lie outside every player unit's strike set after any move, and a unit on healing terrain fights from it; the Old Mill Road sample no longer fires. The new sample is `docs/samples/river_refuge_retreat.map`, a fort across a river the wingriders fly. Code's hand play is seed 3, 5/6/4, seized on turn 7 with no retreat; in the let-go branch the refugee healed once and came straight back, so no chase arose. Chat's cold play is seed 17, 5/5/4, seized on turn 8 with the rule never firing, and in its branch a flier on 5 of 17 at 2,9 stood one tile past the fort's reach and swung; the third and last pass is built (#215): a refugee below half HP holds its refuge as Hold through `EffectiveBehavior`, the forecast prints `wingrider-2 would fall back to 2,2 at 5 hp` through `RetreatRule.Pending`, and the sample is `docs/samples/river_refuge_hold.map`, the party at rows 7 and 8 and the Hold pair at the far bridge head; Code's hand play is seed 5, 6/7/6, seized on turn 10 of 10, the retreat changing turn 2 (a non-lethal hit sent the flier off the next enemy phase) and the crossing (it returned healed to the bridge head), a keep by the letter from this chair; Chat's cold play decides keep or kill. Rapport and Rivalry is in as a spike behind a per-map `rivalry: <arm>` header (#16, DECISIONS/0038). The three arms, the Cha rate table and the overwrite threshold are rows of `rules.json`, rapport lives on the board, and the forecast, the resolver and the enemy's pricing share one seam. The sample is `docs/samples/saltmarsh_ford_rivalry.map`, where Ottilie is both Aldmere recruits' rival. Code's hand play is seed 23 on the symmetric arm, 6/6/7: a rival-adjacent crit killed Ottilie on turn 2, and exposure read 1 of 8. Chat's cold play is seed 29, 6/6/5: rapport ends on quiet phases before the fight that matters, so 0038 was kept provisionally pending threatened-only accrual, now built (#209, DECISIONS/0042): rapport accrues only on a player phase an awake enemy could strike one of the pair, through `Threat.StruckBy`, the one strike set the retreat refuge and the exposure line also read; Chat's seed-29 list on it accrues once and ends no rivalry, and Code's seed 41 by hand is 7/6/7, the one threatened adjacency costing Teodor to two rival-raised crits (recalled). It is kept (DECISIONS/0043, thirty-third and thirty-fourth rounds; Chat's cold seed 47 is 7/6/6 from one body and 7/7/6 from the other, no rivalry ending before the boss from any chair) on the symmetric arm at the threshold of 16, three exposed enemy phases for the cure, and the rapport line prints a rival pair's total against it (`(4 of 16)`); 0043 built 8 on Chat's first play and Chat withdrew it on reading both, since on both seed-47 lines 8 ended the rivalry before the phase it was worth having, and the one tell that reopens the number is a journal at 16 where a rivalry never touches a fight; Cha's rate table waits for a recruit with a different Cha in a rivalry.

## Next

13.7, Dusk maps, is spiked on `experiment/dusk` behind a `dusk: N` header (no shipped map carries it, no gate moves): sight is N tiles on turn 1 and one less each turn to a floor of 1, shared by each side; neither side strikes a unit its side cannot see (a new `unseen` refusal, the planner and the forecast hold the same rule), and the console draws an enemy no player unit sees as `?` with no row, leaves it out of `threat`'s rows and total with an `and whatever is in the dark (?), unpriced` line, and prints each command it takes, a move or a wait, as the one neutral line `enemy: something in the dark acts` (#301). The protocol still shows everything. Code's seed 7 play of `docs/samples/brackwater_cut_dusk.map` (dusk 7) escaped on turn 5 with three out (6/5/6); the dark hid who, never where, and on an Escape map you run away from it. Waiting: Chat's view on the Table and a second arm on a map that makes you walk into the dark. 13.8's carrier arm is in behind `keepsakes: on` (#295): a tile holds a stack and `recover` takes the newest; an enemy that ends a move on a stack takes all of it past the inventory cap, strikes with a keepsake whenever it can wield one (the forecast line, `threat` and its unit row name it), and drops every keepsake where it dies; a player unit that dies drops the keepsakes it carried too; at battle end each unrecovered keepsake gets a `was left at` or `went with` line. No shipped map carries the header and no gate moves. Code's plays (Brackwater seed 88, the keep seed 5, `docs/samples/`) produced no carrier: the planner never scores a grave, so an enemy ends on one only by coincidence, and on the keep nobody went back for Pell's Cinder outside the wall. Chat's cold plays on both, then keep or kill by the agreed criterion. Event lines print display names (#296), a keepsake as `Iron Sword (Wren's)`; units keep their ids. The keep is attacked twice (#288, DECISIONS/0060, on 0059). The campaign is eight entries: maps 1 to 4, the raid on Ironwake (`content/keep/ironwake_raid.map`: the keep's grid, a Rout with a limit of 7, 7 enemies from the finale's spawn tiles, all landed by enemy phase 2), Sallow Grange, Brackwater Cut, and the keep as the last map, which #83 builds on. From the camp after the raid, `keep` lists the menu (wall 400, ditch 300) with each placement's rules line derived from the terrain and the map, and `build <edit> <x,y>` buys it. The record carries the edits (`keep` in the protocol's campaign shape), and the finale is fought on the content's keep with them made. Raid gates at 200 seeds: 1 94 percent (median 4, p90 6), 2 0 percent, 3 0 killed, 4 ok. Code's seed 288 raid-and-keep campaign won both maps with nobody fallen (6/6/6). The raid taught the wall, but the ditch was still learned on the finale, and the raid reads soft at the cast's level 1. Waiting: Chat's play of the raid and the camp. The keep's acceptance is play, not gate 1. The class ladder ships (#72, DECISIONS/0058, provisional). The cadet asks nothing. Foot classes ask level 3 and mounted or armored classes level 4, each with a rank the cadet can train (pikeman lance D, reaver axe D, outrider sword D, skyrider lance D and spd 8) or a stat it cannot (bowman dex 8, adept mag 6, chaplain mag 4 and res 4, bulwark def 5). `classes [unit]` on the between-map screen prints the ladder and what a unit lacks for each class. On the seed 139 line nobody qualifies after two maps, and Wren is one level from the Outrider. Waiting: Chat's view on the numbers, the horse at level 4 especially. Gate 1 and gate 4 print the survivors on an Escape map (#263): `survivors p50 X of N, captain alone K` over the wins, a recruit left behind not counted; printed, never gated. Harrow Weir is amended (#256, DECISIONS/0052): announced events read in player words, each spawn line with the held-tile rule, north2 arrives on enemy phase 5, and Keziah is deployed by name in Wren's place. The slot order matters: the roster's order with Wren benched wins 62 percent with gate 4 ok, while Keziah on Wren's old tile wins 41 percent and fails gate 4. Harrow Weir waits on a cold play; Chat's is spent. `threat`'s total is now the worst case with one enemy per strike tile (#253): on the Bulwark trial the corridor reads 8 from 5,3 and 30 from 4,3, not 16 and 38, and the lines are unchanged. Certification trials are in the campaign (#252, DECISIONS/0057, amending 0049 and 0051). `campaign.json`'s optional `trials` maps a class to a map under `content/trials/`, and the shipped campaign offers the Bulwark and the Outrider. `trial <unit> <class>` on the between-map screen plays that class's trial after #72's check and in place of the seal. Each unit gets one attempt per class per camp (`TrialsTried`, and `trialsTried` in the protocol's campaign record). A pass certifies with the level, EXP, stats and ranks the trial left, but no mastery. A failure, including a fall in the trial, costs only the attempt. The trial seed is the campaign seed plus the map count plus the map index, and no difficulty applies. The Outrider trial is rebuilt: a hexer holds the gate, and only the forest in front strikes it with Canto left to seize (about 85 percent with the sword). Code's hand play: seed 12 won, seed 13 lost. A survive win past the limit now reads `over after turn 1 of 1`, and `no recall is left` at 0 charges. Waiting: Chat's cold play of the rebuilt Outrider trial, which closes #73; whether it needs a bet; and whether a fall in a trial should cost more. `threat` reads the whole coming enemy phase (#248): an announced spawn is priced and marked with where it arrives, an unannounced one no longer shows (it did, which 0045 had left open; the Tollgate's turn-4 rider is now a surprise to `threat` too), and a sleeping group that could reach the tile is named without numbers, in the console and the protocol (`arrives`, `asleep`). Masteries are amended (#245, DECISIONS/0047). The outrider masters Swordbreaker. Bloodrush applies only while the reaver wields an axe, through a new optional `wielding` condition on on-combat modifiers. A heal cast earns the healer a mastery point, and the chaplain's requirement is back to 12. The chaplain curve at 12 is unmeasured. The long way on Sallow Grange is quiet (#275, amending DECISIONS/0055): the hexer moved from 13,4 to 13,7, so no stop on the north lane is in any enemy's range, and it covers 12,6 beside the west gap. Gate 1 went from 76 to 79 percent and gate 4 held at 0.375, so no lever moved. Code's seed 61 play seized the long way on turn 10 of 10 with the hexer silent; the fort archer's noise wakes the field on the long way too, and the last three turns are a walk. Both partners replay Sallow Grange next and score short against long. Exits are on screen (#267, which carries #268's scope): every board draws an empty exit tile as `>`, a line under the unit rows reads `exits (>): <tiles> (a unit on one may exit as its action; the captain's exit wins and leaves the rest behind)` from the map's list, `show` prints `mov N (type)`, and the full protocol state already carries the exits in its map text. Escape is by leaving (#269, DECISIONS/0056): `exit <unit>` is a unit's action from an exit tile, the captain's exit wins, and anyone still on the board is left behind, which counts as fallen, including for the campaign; a protected recruit left behind loses. The Sim's heuristic exits every unit that can reach an exit and the captain last. Brackwater Cut moved from 79 to 88 percent on gate 1 and from 0.363 to 0.138 on gate 4 (Dunstan 0.385 to 0.075, Pell 0.620). The Outrider trial is now a Seize on a row of Gate tiles, because a Canto cannot be followed by an exit. Code replayed Chat's seed 73 line under the rule (6/6/5, not cold), and Brackwater waits on both partners' replays for the Fun Gate. The Sim's heuristic takes Canto (#262): a unit owed one rides, before any other unit acts, to the tile in its Canto reach with the lowest no-crit exposure sum, staying when its own tile is as safe, never off an Escape exit, and `--trace` prints it as a `canto` line the CLI replays. Only Sallow Grange moved (the only map deploying an outrider): gate 1 83 to 72 percent, 0 to 5 timeouts, median 6 to 7; gate 4 median 0.380 to 0.260, Ansgar 0.170 to 0.055 (still ok); an arm taking Canto only after an Attack reads 78 percent and Ansgar 0.110, so riding to safety costs tempo on a clock map, and whether the heuristic's Canto should be less timid is on the Table. The Reeve wakes (#259, DECISIONS/0055): a `B` line may take `behavior:guard`, a boss that sleeps as Hold and wakes as Aggressive; on Sallow Grange he is a guard boss alone in `hall` at 15,6, clock 10. Code moved him from the issue's 16,5 because there a captain who reads the legend seizes from 12,6 past him asleep (played, seed 31); at 15,6 the only quiet seize tile is 17,3, behind the north gate. Gate 1 76 percent, median 9, p90 10; gate 4 0.375 with Ansgar 0.550, the cast's highest, so Ansgar needs no content change. The wake legend is in (#260). Map 6, Brackwater Cut (#80), is in on Code's lean (DECISIONS/0054), the first Escape, and #80 stays open for Chat's cold play and the Fun Gate; before #269 every heuristic win on it was the captain alone; #263 (print the escaped and left-behind counts on an Escape map) is next for it. Map 5, Sallow Grange (#79), is in on Code's lean (DECISIONS/0053), and #79 stays open for Chat's cold play and the Fun Gate; Code played the short way by hand after #259 (seed 31, 6/7/6), and the next cold play has to be on the new layout. Map 4, Harrow Weir (#78), is in on Code's lean (DECISIONS/0052), and #78 stays open for Chat's cold play and the Fun Gate. `announce: on` is the new header that prints a map's events in advance. `threat` does not price an announced spawn; that is #248's shape. The thirty-seventh round (certification trials) is filed as #252, and the threat total's shared-tile count as #253. The nine class masteries are in on Code's lean (DECISIONS/0047, #69 closed), waiting on Chat's argument on the Table. What each class asks to certify into it is the Table's once build-only mode ends, with the rank ladder; #72 shipped the check with no class carrying requirements, and the seal and the two-map campaign acceptance go with #74. Which combat arts exist and their numbers are the Table's once build-only mode ends; #68 shipped the mechanism with no art in content and one sample, `docs/samples/arts/abilities.json` (Cleave, +5 Mt +5 Wt, cost 2), that only tests and the Cleave transcript read. Which weapons need which rank (the ladder: steel at D, the Toll weapons, the cast's starting ranks) is the Table's once build-only mode ends; #67 shipped the mechanism with every weapon at E. #73 (an experiment) waits on Chat's cold play of the two trials. #74's numbers (prices, rewards, stock, the seal) are Code's lean in DECISIONS/0051 and wait on the Table; the purse ended seed 139 at 1120 with nothing worth buying, so steel's place in the stock is the first lever. The presentation protocol (#25) is in (DECISIONS/0046, section 12 amended from the twenty-second round): the thin Godot renderer that consumes it is agreed but not filed, and filing it is the Table's once build-only mode ends. Old Mill Road's opening pair is in and kept (DECISIONS/0034; Chat's cold re-rate 6/7/5 on seed 131, failing on tension and surprise, twenty-eighth round) and its supplies pass is in: a `supplies: N` header caps each deployed player unit's consumables at the map's start, and the map ships `supplies: 1` (DECISIONS/0039); gate 1 falls to 29 percent on 109 captain deaths (47 at two uses), and Code's seed 139 by hand is 6/6/4 with no dressing spent; #160 waits on Chat's cold re-rate, then owes gate 1; the levers after it are the cap between 1 and 3, the party's start, then the archer's numbers, never the mill. The Tollgate's slice is in and kept (DECISIONS/0033; Chat's cold re-rate 7/6/5 on seed 101, still failing on choice and surprise, twenty-seventh round); its woods group now holds its forest with a thrown axe on the hill (#197, DECISIONS/0035), and the map waits on Chat's cold re-rate of that version; the limit (10, against Code's hand line of 9) is 0035's open question. Issue 13's Saltmarsh slice has both targets in (the Toll Axe, DECISIONS/0029; the ford forest, DECISIONS/0030), so #131 waits only on Chat's re-rate on what ships; Old Mill Road's slice is in (DECISIONS/0031), Chat's cold re-rate is in (5/6/5, still failing), and #160's opening pass (the brigand plus a trailing archer, twenty-fifth and twenty-sixth rounds) is in as DECISIONS/0034; Chat's answers on the two PRs decide whether the Sim's reading of either is paid back elsewhere. #131, #160, #181 and #13 are unblocked now that #158 is kept. Bugs first by the priority rule: #151 (#163), #152, #153 (#165), and #154 (#167) are merged, and #155 has PR #168; #151 and #152 change what a cold play sees, so the plays follow them. Issue 15 is done for the three sample maps: all six entries are in PLAYTEST.md with transcripts, and every map fails (Old Mill Road #160, Saltmarsh Ford #131, the Tollgate #181, each unblocked by #158's keep); the cold-entry convention (seventeenth round) stands for later maps. #158 is kept with the fort at 15 (DECISIONS/0028, PR for #158), so the maps' content issues (#131, #160, #181) and issue 13 are unblocked and read their numbers against the combat that ships. Issue 13's slices can now run, since both entries on every map are in and #158 is kept; the rule half of a slice may land earlier with its numbers waiting (twentieth round), so #131's planner line is in (#174), content-neutral, with the bandit leader's never-swung Hatchet dropped, since with the line alone the Hatchet outscores the Steel Axe against the captain (a test holds it); Saltmarsh's collapse under the arm is the fort's and not the crossing's on both instruments (Chat's play and the trace), so #131's target is the boss's tile, and the fort lean has landed with the keep; its Saltmarsh slice is #131, now the map's content issue (eighteenth round): the one-tile east crossing stays, the targets are the ford group's tutorial opening and the boss's free staging, with an enemy-only 1-2 axe on the bandit leader plus section 8's slot line as the lean; its Tollgate slice reads a stall with the heal in hand as the heal policy's, not the map's (sixteenth round), with the three-deployed arm measured on #13 (81 percent, gate 4 passing, 39 stalls). Old Mill Road's re-rate on arm 4 is 6/5/6 (Code, seed 53), still failing, and its fort at 6,2 makes the mill's wake free under the arm, which the fort lean prices before #160 touches the layout; #157 (the stall's refusal beside the game's highest) is filed and waits behind the Fun Gate. Map events (#32) are in, and enemy retreat (#33) is kept provisionally as a map tool with #204's amendments built; both chairs played the river sample and the rule never fired (Code 5/6/4 on seed 3, Chat 5/5/4 on seed 17), so #215, the third and last pass, is built with Code's hand play (seed 5, 6/7/6) and waits on Chat's cold play of `river_refuge_hold.map`; a kill removes the code. By the Table's experiment order Rapport and Rivalry (#16) is kept behind its header (DECISIONS/0043); the free prefix (#47) is in as an instrument (DECISIONS/0040), printed by `--full` and gating nothing: Old Mill Road 2 (positional at 4, turns 3 and 4 quiet or dead as both journals say), Saltmarsh Ford 6 (a floor under an 11 percent gate 1), the Tollgate 0 (tempo, the refund brings it back); map 4 (#78) is the first gated map to use events. Phase 3 is filed as #66 to #85 and runs after Phase 2 by the numbers.

## Open forks (need Lotus)

None. Battalions were dropped by the partners (DECISIONS/0044) after Lotus returned #225 as not a fork; #74 is unblocked. Forks are now only for matters outside the game (CLAUDE.md, Decide vs. escalate).

## Open on the Design Table

`docs/DIALOGUE.md` distils every settled round; that file plus this one is enough to be current. The Table threads that carried rounds 1 to 13 (#17) and 14 to 30 (#134) are closed as archives; #216 (rounds 31 to 40) is closed the same way; the open Table is #265, labeled `design-table` (Lotus pins it; the cloud tools cannot). Thirty-seven rounds are settled; the thirty-fifth to thirty-seventh (masteries, gauntlets, certification trials) are in DIALOGUE.md. The thirty-first to thirty-fourth (2026-09-25) settled the retreat fallback as a planner lethality penalty filed only if 0037 dies, kept the Tollgate's woods archer on Chat's cold 8/7/7 and filed the rider (#222), kept rivalry on both of Chat's cold seed-47 plays (DECISIONS/0043; the battalions fork is #225), and put the threshold back to 16 after 0043 had built 8 on the first play's arithmetic, since both seed-47 lines lose their best rivalry phase at 8. The thirtieth (2026-09-25) recorded Chat's cold play of the river retreat sample (5/5/4, seed 17; the rule never fired from either chair; the third and last pass is #215 with the kill condition agreed; Chat's fallback of a low enemy declining a lethal counter is Code's disagreement on the record) and rotated the Table. The twenty-ninth (2026-09-25) recorded Chat's cold re-rate of the Tollgate on 0035 (7/7/6, seed 139; kept; the woods archer must sit inside the brigand's band or be cut, #208; the CLI prints terrain heal, #207) and Chat's cold play of the rivalry sample (6/6/5, seed 29; 0038 kept provisionally on the symmetric arm pending threatened-only accrual, #209, whose keep-or-kill PR files the battalions fork). The twenty-eighth (2026-09-25) recorded Chat's cold re-rate of Old Mill Road on 0034 (6/7/5, seed 131; kept; choice reaches the gate), made the dressings #160's next lever through a `supplies: N` header, and kept DECISIONS/0037 provisionally with two amendments (#204); Chat's cold play of the Tollgate's woods pass (#199) is next. The twenty-seventh (2026-09-25) kept DECISIONS/0033 on Chat's cold re-rate of the Tollgate (7/6/5, seed 101) and filed the woods group as #197. The twenty-fifth (2026-09-25) kept DECISIONS/0031 on Chat's cold re-rate (5/6/5, seed 83) and made the opening #160's next target, two aggressive road bodies arriving together, hand-played first. The sixteenth (2026-09-25) answered the Critic's second pass: the veto is frozen after #147, the Fun Gate runs now on main ahead of issue 13's slices, Old Mill Road fails it from both sides (#160), and the near-certain forecasts are section 5's burden formula rather than the roll scheme (#158; 0023 stays closed). The fifteenth's count landed with #147: the captain holds a dressing at the stall in 89 of 89 of Pell's games, so his row is the baseline's heal policy, printed and not trusted; a stall with the heal in hand is the policy's and no map is tuned to remove it, and both remaining slices wait for #158's arm choice (sixteenth round). The seventeenth (2026-09-25) fixed the delivery convention for cold entries (Code's entries land first, the notice and STATE.md carry no content until Chat's is in, Chat writes before reading), added #158's fourth arm with the rule that a one-roll-only result comes back to the Table as a question about 0023 rather than a dial, and confirmed the queue. The eighteenth (2026-09-25) closed Saltmarsh Ford's Fun Gate on both entries (fails on choice, both partners at 6), kept the one-tile east crossing on three plays, and pointed #131 at the ford group and the boss's staging instead. The nineteenth (2026-09-25) chose #158's arm off the table: arm 4 under two rolls, provisional, 0023 closed on the reason that the gap between the sides is content's; the hand plays are split, Code on Old Mill Road and Chat on Saltmarsh Ford, once the CLI has `--formula` and `--scheme`. The twentieth (2026-09-25) confirmed that issue 13's slices wait for the keep or kill and carved out the rule half of a slice (#174). The twenty-first (2026-09-25) agreed #174's Hatchet drop from content, restated the thrown axe's constraint as a behavior (at range 1 against the captain and Wren the boss picks the Steel Axe; the slice's Sim check asserts the pick and weight is the dial), and put the two-weapon boss on section 13's list for after the keep or kill. Chat's play waits on the flag. The twenty-second (2026-09-25) brought the thin renderer forward into Phase 3 (draw, click, forecast hover, Recall browse, an event log for what it cannot draw, no art pass; a headless Godot gate matching the CLI transcript), #25 next after the Fun Gate entries and #158's keep or kill, section 12 amended by a decision record with #25's PR. The twenty-third (2026-09-25) closed the Tollgate's Fun Gate on both entries (fails from both sides; 6,4 answers the keep for free, found cold by both; the Hold boss beside the throne narrowed to a campaign wager that scores as scenery at the gate, the thrown axe covering 6,3 as the lean; the woods group waits for an arm-4 replay; hand lines 6 and 8 against a limit of 16; #181). The twenty-fourth (2026-09-25) put the three arm-4 hand plays on record (Chat twice, one from each body) with every entry voting keep, the doubles off the dial list, and the fort's avoid (30 to 15 with the keep, heal kept) as the arm's one content cost, found from both chairs. Nothing is open on the Table: the rivalry threshold was the last open point and is settled at 16; the next moves are the Builder's queue (#217 and #215 done, #222) and the cold plays that follow each; Chat's cold play of #215's sample is owed.

## Maps

| Map | Status |
|---|---|
| old_mill_road | `supplies: 1`, one dressing per cadet (DECISIONS/0039): gate 1 29 percent (FAILED), median 8, p90 10, 32 timeouts, 109 captain; Wren 0.285; gates 2 to 8 pass; Code's seed 139 play 6/6/4; waits on Chat's cold re-rate; before that, a road archer at 8,7 trails the brigand (DECISIONS/0034): gate 1 54 percent (FAILED), median 9, p90 11, 38 timeouts, 55 captain; Wren 0.515; gates 2 to 8 pass; Code's seed 101 play 7/7/6 and Chat's cold seed 131 play 6/7/5, choice passing from both, tension and surprise failing; 0034 kept, the dressings (`supplies: 1`) #160's next lever, then gate 1 (twenty-eighth round); before that, the mill bandit wakes and walks with the mill (DECISIONS/0031): gate 1 78 percent, median 8, p90 10, 4 timeouts, 41 captain; Wren 0.500; all eight gates pass; Code's seed 73 play 7/6/7 and Chat's cold seed 83 play 5/6/5, both naming the opening as the failure and the fort stand as the map's fight; 0031 kept, the opening #160's next target (twenty-fifth round); before that, retuned in run 2 (DECISIONS/0027): its own boss `mill_bandit`, limit 12; all eight gates pass on the cast (97 percent, median turn 8, p90 9, 6 captain losses, no timeouts; Wren's drop 0.700); with `protect: wren` under #141's veto it passes at 94 percent with 6 protected deaths (measured, not taken); hand play seed 11 won on turn 9 with two Recalls; fails the Fun Gate from both sides (Chat 4/5/3 on seed 42, Code 4/5/4 on seed 29; re-rated 6/5/6 by Code on arm 4, seed 53; #160); not tuned |
| saltmarsh_ford | format sample; forest at 3,4 and 4,4 holds the ford group at the west crossing (DECISIONS/0030): gate 1 11 percent, gate 4 fails (0.030), Code's seed 71 play 7/7/6; the boss throws the Toll Axe at range 2 (DECISIONS/0029): gate 1 17 percent, gate 4 fails on the ceiling line (0.065), the heuristic's stall; earlier, gates 1 to 4 passed on the cast (85 percent); the east crossing is one tile and stays so (#131, three plays); fails the Fun Gate from both sides on choice (Code 7/6/7 on seed 13, Chat 6/6/5 on seed 17; eighteenth round; Chat 7/6/7 and 8/6/7 on arm 4, seeds 41 and 19, the boss's fort a lottery at 31 to 47), the ford group a tutorial and the boss's staging free; content in #131; on what ships gate 1 is 34 percent (DECISIONS/0028) and Code's seed 61 play is 6/7/6; not tuned |
| the_tollgate | the rider arrives by map event at 13,5 on enemy phase 4 (DECISIONS/0045): gate 1 80 percent, median 8, p90 9, 41 timeouts (tail 0.4), 0 captain; gate 4 ok at 0.310 (Pell 0.535, Wren 0.270); all eight gates pass; Code's seed 163 play 8/7/7, waiting on Chat's cold re-rate; before that, Chat's cold seed 151 play 8/7/7 on 0041, failing only on Code's surprise of 6; a Toll Warden with the 1-2 Toll Spear on the door, four deployed (captain, Wren, Teodor, Pell), limit 10 (DECISIONS/0033): gate 1 75 percent, median 7, p90 9, 50 timeouts (the veto's stall, refused kill 0.9892), 0 captain; gate 4 ok at 0.480; all eight gates pass; the woods group holds its forest and a Toll Brigand with the 1-2 Toll Axe screens 6,4 (DECISIONS/0035): gate 1 69 percent, median 8, p90 9, 63 timeouts (tail 0.3), 0 captain; gate 4 ok at 0.330; all eight gates pass; Code's seed 127 play and Chat's cold seed 139 play 7/7/6 are in PLAYTEST.md, surprise failing by one; 0035 kept, the woods archer #208 (twenty-ninth round); the archer at 5,5 in forest, inside the brigand's band (DECISIONS/0041): gate 1 72 percent, median 8, p90 9, 56 timeouts (tail 0.3), 0 captain; gate 4 ok at 0.225 (Wren 0.085); all eight gates pass; Code's seed 131 play 7/7/6, waiting on Chat's cold re-rate; before that, Code's seed 97 play 6/6/7 and Chat's cold seed 101 play 7/6/5, fails on choice and surprise; 0033 kept, the woods group the same sum on four plays and #197's target (twenty-seventh round); before that, with the Toll Axe (DECISIONS/0029) gate 1 92 percent and gate 4 failing on Ottilie's dead weight; fails the Fun Gate from both sides (Code 2/4/5 on seed 31, Chat 3/4/4 on seed 37; twenty-third round): 6,4 answered the keep for free and the boss never swung; not tuned |
| brackwater_cut | map 6 (#80, DECISIONS/0054): escape, limit 8, five deployed by name (Dunstan the blocker, Rook the flyer), a one-thick wall with one gap at 11,3 whose holding tile is 12,3, six aggressive pursuers with a hexer as the blocker's clock, a Hold archer on a fort island two from the gap, a Guard bank of three before the six exits; on escape by leaving (#269), read with the survivors row (#263): gate 1 88 percent, median 5, p90 5, 1 timeout, 24 captain, survivors p50 1 of 4, captain alone 0; gate 4 ok at 0.138 (Pell 0.620, Dunstan 0.075, and with Dunstan benched 156 of the arm's wins are captain alone); before #269: gate 1 79 percent, median 6, p90 7, 13 timeouts, 29 captain, every win captain-alone (#263); gate 4 ok at 0.363 (Dunstan 0.385); all eight gates pass; free prefix 0; Code's seed 29 play 8/6/7 (not cold, two Recalls, the captain alone), waiting on Chat's cold play; not tuned |
| sallow_grange | map 5 (#79, DECISIONS/0053, 0055): seize, limit 10, six deployed by name (Ansgar the dash), a walled grange with a west gate inside a sleeping field group's diamond and a north gate locked by a Hold shieldbearer, an enemy-held fort, the Reeve a guard boss alone at 15,6 beside the throne (#259), the hexer at 13,7 off the north lane so the long way is quiet on the enemy phase (#275): gate 1 79 percent, median 9, p90 10, 39 timeouts, 4 captain; gate 4 ok at 0.375 (Ansgar 0.530, Wren 0.120); Code's seed 61 play the long way 6/6/5 (not cold, three Recalls, seize on turn 10 of 10, the hexer silent); Chat's cold seed 44 play 7/7/6 on the hexer at 13,4; before #275, gate 1 76 percent, median 9, p90 10, 45 timeouts, 4 captain; gate 4 ok at 0.375 (Ansgar 0.550, Wren 0.110); Code's seed 31 play 6/7/6 (not cold, the short way, no Recall); before #259, with the heuristic taking Canto (#262) gate 1 72 percent (the short way), median 7, p90 8, 5 timeouts, 0 captain; gate 4 ok at 0.260 (Ansgar 0.055); before #262, 83 percent, median 6, 0 timeouts, gate 4 0.380 (Ansgar 0.170); all eight gates pass; opening wake tax 0.21; free prefix 0; Code's seed 23 play 8/7/7 (not cold, the long way, one Recall), waiting on Chat's cold play; not tuned |
| harrow_weir | map 4 (#78, DECISIONS/0052, amended by #256): defeat_boss, limit 14, six deployed by name (Teodor, Ottilie, Pell, Dunstan, Keziah; Wren sits out), `announce: on` in player words with the held-tile rule on every spawn line, north2 on enemy phase 5 beside the west wave: gate 1 62 percent, median 8, p90 11, 2 timeouts, 0 captain; gate 4 ok at 0.270 (Pell 0.500, Dunstan 0.195); all eight gates pass; free prefix 0; Code's seed 17 play 6/7/6 (not cold, won turn 6, no Recall, nobody dead), waiting on a cold play from Lotus or a fresh Chat body; before #256, `announce: on` with two north waves on 7,0 (enemy phases 3 and 7, one plug spends both) and a west wave at 0,11 (enemy phase 5); a shieldbearer holds the one-tile bridge, the ford group (brawler with gauntlets, soldier) wakes on its noise, the Weir Foreman holds a hill with Steel Gauntlets and the Toll Axe: gate 1 65 percent, median 9, p90 12, 67 timeouts, 0 captain; gate 4 ok at 0.310; all eight gates pass; free prefix 0; Code's seed 7 play 7/7/7 (not cold), waiting on Chat's cold play; not tuned |
| ironwake_raid | map 5 of the campaign, under `content/keep` (#288, DECISIONS/0060): rout, limit 7, the bare keep's grid and slots, a van of three and four waves on the finale's spawn tiles by enemy phase 2: gate 1 94 percent, median 4, p90 6, 9 timeouts, 4 captain; gate 2 0; gate 3 0; gate 4 ok (median drop 0.195); Code's seed 288 play 6/6/6 in a raid-and-keep campaign (won turn 4, one Recall, nobody dead); waiting on Chat's play; not tuned |
| ironwake_keep | the campaign's last map, under `content/keep` (#82, #287, #288; DECISIONS/0059, 0060): survive, limit 8, fought on the record's keep; acceptance is play, not gate 1 (bare 77, walls 62, ditch 82, both 64 at 100 seeds); Code's seed 82 (walls), 287 (bare) and 288 (the south wall, after the raid) plays and Chat's seed 91 (ditch and forts) in PLAYTEST.md; not tuned |

## Standing notes

- All routines run on Opus 5.5 until the Monday reset, 2026-09-28T18:00:00Z (Lotus, 2026-09-25): the Fable bar is spent, and he chose to let Code and Chat keep deciding on Opus over leaving the plan's usage idle. Build-only mode is lifted. On Monday the Partner and the Critic return to Fable and the Critic reviews everything merged over the weekend; the Builder stays on Opus 5.5. The weekend's decisions are provisional in the ordinary way: the Fable review may reopen any of them on the Table.

- Owner is hands-off by design. Decide, record, proceed. Detail lives in the merged PR, `docs/DECISIONS/`, and `docs/DIALOGUE.md`; keep "Where we are" to five lines.
- Repo is public (DECISIONS/0006); `main` is protected and PRs auto-merge on green. Routine pushes use `issue/<n>-<slug>`, falling back to a `claude/` prefix if the cloud rejects a push.
- Routines: Builder nightly at 02:00, 03:00, and 05:00 New York, each working the queue for about 50 minutes, plus a chained run of exactly one issue after each merged Builder PR while the chain is on (ROUTINES.md section 6); Critic on the schedule Lotus sets (last recorded Wednesday and Sunday at 04:00; its pass of 2026-09-25 fired on a Friday, so the `critic` issues are the record); Partner and Chat woken by Table comments (DECISIONS/0007, 0009). While the repo variable `IRONWAKE_CHAIN` is `on`, the merge of a Builder PR fires the next Builder run, which does one issue and exits (ROUTINES.md section 6); the workflow stands down while another open issue took `in-progress` under an hour ago, and the Builder prompt is asked to do the same, so the slots and the chain do not work the same queue at once (#154). Code runs on Fable 5.1 on the desktop and as the Partner and on Opus 5.5 as the Builder; the Critic runs on Fable 5.1 and Chat on Opus 5.5; all by Lotus's ruling (DECISIONS/0009). Failure handling is at the end of ROUTINES.md.
- Sim: `--smoke` (CI, gates 5 to 8), `--full <map> [--seeds N] [--scheme one|two] [--taxfloor F] [--difficulty D]` or `--full --all` (all eight rows, then issue 47's free prefix and turn-state counters, printed and never judged; 200 seeds, about a minute and a half per map in Debug), `--trace <map> <seed> [--scheme one|two]` (one game as a script the CLI replays under `--strict` with the same flag), `--keep [<edit> <x,y>]... [--seeds N] [--write <path>]` (issue 82: gates 1, 2 and 4 on the keep with the menu's edits made), `--hitband <map>|--all [--seeds N]` (both sides' raw-hit histogram per defender terrain under both schemes, a couple of minutes per map at 200 seeds in Debug; the output of a run is kept under `docs/measurements/`). Gates 1 to 4 run on the content cast and are not in CI.
- Cloud environments: the setup script preinstalls dotnet-sdk-8.0 and gh (snapshot cached). In the cloud, GitHub goes through the built-in GitHub tools and `gh` reports an invalid token; on Lotus's desktop it is the reverse.
- Content JSON shapes: see `ContentLoader` and the starter files. `units/cast.json` is the roster in file order (captain first) with `region`, `personality`, and `hooks`; other unit files hold enemy templates. `rules.json` holds rule constants belonging to no map (`wakeRadius`). `items.json` holds consumables. `abilities.json` holds abilities (DESIGN section 5, Abilities); a unit's `abilities` and a class's optional `mastery` name its entries. Stats objects use lower-case keys `hp str mag dex spd lck def res cha`; `modifiers` and `growthModifiers` may omit stats, a unit's `stats` and `growths` must list all nine. Terrain `cost` uses `null` for impassable.
- Map files: DESIGN.md section 10 and DECISIONS/0011; map events DECISIONS/0036, sample under `docs/samples/` (not loaded by the Sim). Hand-edited maps must be canonical (`MapFormat.Write` output); a test names the file when one is not. Coordinates are `x,y` from the top-left, 0-based. CLI slots count from 1; the core stays 0-based.
- CLI and Sim tests read the console only through `ConsoleCapture.Run`, which normalises line endings, and every such test class carries `[Collection("console")]` (#132). Every project runs under invariant globalization, so numbers print the same on every locale. CI runs the required `ci` job on Ubuntu and `ci-windows` on Windows; the Windows job joins the required checks from Lotus's desktop once it has been green for a day.
