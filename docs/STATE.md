# STATE

Updated: 2026-09-25. Rewritten, not appended: this file is what is true now, not what happened.

## Where we are

Phase 1 of DESIGN section 12 is complete and Phase 2 is most of the way: rules, content loading, maps, movement, combat on keyed rolls, battle state with history and Recall, the turn loop, EXP, items, the enemy AI, the playable CLI, and the Sim harness with gates 1 to 8. The cast of eleven is in content. `ironwake play <map> [--seed N] [--script f] [--strict]` plays a real battle and both partners have played by hand, and its forecast answers from any tile a unit can still move to (#151), so a tile is chosen with its numbers in view; the three sample maps pass gates 1 to 4 on the cast but none is `tuned`; Old Mill Road has both Fun Gate entries and fails it (Chat 4/5/3, Code 4/5/4; sixteenth round, #160), Saltmarsh Ford has both and fails it on choice (Code 7/6/7 on seed 13, Chat 6/6/5 on seed 17; eighteenth round, #131), the Tollgate fails it from both sides (Code 2/4/5 on seed 31, Chat 3/4/4 on seed 37; twenty-third round, #181), so every sample map has both entries and fails, and issue 15's first pass is done. 643 tests green on Linux and, from #132, on the `ci-windows` job too. The hit A/B is settled on two-roll averaging (DECISIONS/0023). The exposure sum counts the board the command certainly produces: a certain kill removed (#117), a strike at raw hit 0 dropped (#126), and a group the command wakes counted by its move through the wake predicate the resolver shares (#128, DECISIONS/0026). Issue 13's run 2 has started: Old Mill Road is retuned against the cast with its own boss and a limit of 12, all eight gates passing (DECISIONS/0027). The heuristic's veto covers every unit whose death loses the map (#141): the captain and a `protect:` recruit, through one predicate, and the core refuses to bench either. Gate 1 and gate 4 print the refused-kill median over their timeouts, floored at four decimals so only a certain kill prints 1.0000 (#153), and gate 4's rows the benched arm's losses by cause (#125); the number counts the counter between the strikes on every path through the second strike (#147); on the Tollgate the stall board reads 0.72 but Pell's row still prints 1.0000, since the game's highest refusal is a turn-6 gate board, and the captain holds a dressing in all 89 of his stalls, so his row is printed and not trusted (fifteenth round). The veto is frozen after #147 (sixteenth round): further baseline work is filed and waits behind the Fun Gate. The Sim's `--hitband` prints issue 158's table: every arm under both roll schemes with both sides' raw-hit histogram, the doubling rate, and gates 1 and 4, on a `CombatFormula` the state carries beside the roll scheme (`Standard` is what ships; the arms exist only for the measurement); the table is measured over 200 seeds (`docs/measurements/`, with a per-terrain row since #158's flag PR) and the Table chose arm 4 under two rolls as the provisional cell (nineteenth round, 0023 closed); `ironwake play` takes `--formula standard|arm1..arm4` and `--scheme one|two` and the Sim's `--trace` both, through one core mapping (`FormulaArms`), so the arm hand plays can start; nothing in section 5 has changed. An enemy's attack options range over every weapon it carries and the winning Attack names the slot (#174, section 8's weapon-choice paragraph); the bandit leader carries the Steel Axe only, so with one usable weapon per enemy nothing the Sim or a hand player sees has changed.

## Next

Bugs first by the priority rule: #151 (#163), #152, #153 (#165), and #154 (#167) are merged, and #155 has PR #168; #151 and #152 change what a cold play sees, so the plays follow them. Issue 15 is done for the three sample maps: all six entries are in PLAYTEST.md with transcripts, and every map fails (Old Mill Road #160, Saltmarsh Ford #131, the Tollgate #181, each `blocked` on #158); the cold-entry convention (seventeenth round) stands for later maps. #158's arm is chosen off the table: arm 4 under two rolls, provisional (nineteenth round; 0023 stays closed, the enemy side comes up in content if it must). #158's flags are in (`--formula`, `--scheme`, `--trace` taking both, the terrain column; the traced Saltmarsh loss under arm 4 is `docs/measurements/2026-09-25-trace-saltmarsh_ford-1-arm4.txt`, posted on the issue and not on the Table, a stall and not a rout); three arm-4 hand plays are in (Chat's Saltmarsh Ford seeds 41 and 19, 7/6/7 and 8/6/7, one from each body; Code's Old Mill Road seed 53, 6/5/6; PLAYTEST.md with transcripts) and all three vote keep (twenty-fourth round), so #158 is `ready` for the Builder's keep with a decision record; the doubles are not the next dial; the arm's one hole is the fort's 30 avoid, found from both chairs, and the agreed lean is fort avoid 15 with the keep, heal kept and the boss's numbers untouched, with #131's throw held back for the Tollgate, measured on gate 1 on a scratch tree (200 seeds: Saltmarsh's gate 1 from 9 to 34 percent with gate 4 passing, Old Mill Road unmoved; the rest of Saltmarsh's collapse is the heuristic's stall in front of the heal). Issue 13 is `blocked` until both entries on a map are in and #158 is kept or killed, since a slice tuned before either is tuned twice; the rule half of a slice may land earlier with its numbers waiting (twentieth round), so #131's planner line is in (#174), content-neutral, with the bandit leader's never-swung Hatchet dropped, since with the line alone the Hatchet outscores the Steel Axe against the captain (a test holds it); Saltmarsh's collapse under the arm is the fort's and not the crossing's on both instruments (Chat's play and the trace), so #131's target is the boss's tile and the fort lean is its first move; its Saltmarsh slice is #131, now the map's content issue (eighteenth round): the one-tile east crossing stays, the targets are the ford group's tutorial opening and the boss's free staging, with an enemy-only 1-2 axe on the bandit leader plus section 8's slot line as the lean; its Tollgate slice reads a stall with the heal in hand as the heal policy's, not the map's (sixteenth round), with the three-deployed arm measured on #13 (81 percent, gate 4 passing, 39 stalls). Old Mill Road's re-rate on arm 4 is 6/5/6 (Code, seed 53), still failing, and its fort at 6,2 makes the mill's wake free under the arm, which the fort lean prices before #160 touches the layout; #157 (the stall's refusal beside the game's highest) is filed and waits behind the Fun Gate. Then 16, 32, 33, 47. Phase 3 is filed as #66 to #85 and runs after Phase 2 by the numbers.

## Open forks (need Lotus)

None. One is expected later: whether battalions stay in scope, filed by the decision PR of whichever Cha spike runs first (issue 16 by the queue); #74 is `blocked` until it is ruled.

## Open on the Design Table

`docs/DIALOGUE.md` distils every settled round; that file plus this one is enough to be current. The Table thread that carried rounds 1 to 13 is issue #17, now closed as an archive; the open Table is the pinned issue labeled `design-table`. Twenty-four rounds are settled. The sixteenth (2026-09-25) answered the Critic's second pass: the veto is frozen after #147, the Fun Gate runs now on main ahead of issue 13's slices, Old Mill Road fails it from both sides (#160), and the near-certain forecasts are section 5's burden formula rather than the roll scheme (#158; 0023 stays closed). The fifteenth's count landed with #147: the captain holds a dressing at the stall in 89 of 89 of Pell's games, so his row is the baseline's heal policy, printed and not trusted; a stall with the heal in hand is the policy's and no map is tuned to remove it, and both remaining slices wait for #158's arm choice (sixteenth round). The seventeenth (2026-09-25) fixed the delivery convention for cold entries (Code's entries land first, the notice and STATE.md carry no content until Chat's is in, Chat writes before reading), added #158's fourth arm with the rule that a one-roll-only result comes back to the Table as a question about 0023 rather than a dial, and confirmed the queue. The eighteenth (2026-09-25) closed Saltmarsh Ford's Fun Gate on both entries (fails on choice, both partners at 6), kept the one-tile east crossing on three plays, and pointed #131 at the ford group and the boss's staging instead. The nineteenth (2026-09-25) chose #158's arm off the table: arm 4 under two rolls, provisional, 0023 closed on the reason that the gap between the sides is content's; the hand plays are split, Code on Old Mill Road and Chat on Saltmarsh Ford, once the CLI has `--formula` and `--scheme`. The twentieth (2026-09-25) confirmed that issue 13's slices wait for the keep or kill and carved out the rule half of a slice (#174). The twenty-first (2026-09-25) agreed #174's Hatchet drop from content, restated the thrown axe's constraint as a behavior (at range 1 against the captain and Wren the boss picks the Steel Axe; the slice's Sim check asserts the pick and weight is the dial), and put the two-weapon boss on section 13's list for after the keep or kill. Chat's play waits on the flag. The twenty-second (2026-09-25) brought the thin renderer forward into Phase 3 (draw, click, forecast hover, Recall browse, an event log for what it cannot draw, no art pass; a headless Godot gate matching the CLI transcript), #25 next after the Fun Gate entries and #158's keep or kill, section 12 amended by a decision record with #25's PR. The twenty-third (2026-09-25) closed the Tollgate's Fun Gate on both entries (fails from both sides; 6,4 answers the keep for free, found cold by both; the Hold boss beside the throne narrowed to a campaign wager that scores as scenery at the gate, the thrown axe covering 6,3 as the lean; the woods group waits for an arm-4 replay; hand lines 6 and 8 against a limit of 16; #181). The twenty-fourth (2026-09-25) put the three arm-4 hand plays on record (Chat twice, one from each body) with every entry voting keep, the doubles off the dial list, and the fort's avoid (30 to 15 with the keep, heal kept) as the arm's one content cost, found from both chairs. Nothing is open on the Table. It is past thirty comments and nothing is live outside DIALOGUE.md, so the next run with budget rotates it.

## Maps

| Map | Status |
|---|---|
| old_mill_road | retuned in run 2 (DECISIONS/0027): its own boss `mill_bandit`, limit 12; all eight gates pass on the cast (97 percent, median turn 8, p90 9, 6 captain losses, no timeouts; Wren's drop 0.700); with `protect: wren` under #141's veto it passes at 94 percent with 6 protected deaths (measured, not taken); hand play seed 11 won on turn 9 with two Recalls; fails the Fun Gate from both sides (Chat 4/5/3 on seed 42, Code 4/5/4 on seed 29; re-rated 6/5/6 by Code on arm 4, seed 53; #160, blocked on #158); not tuned |
| saltmarsh_ford | format sample; gates 1 to 4 pass on the cast (85 percent); the east crossing is one tile and stays so (#131, three plays); fails the Fun Gate from both sides on choice (Code 7/6/7 on seed 13, Chat 6/6/5 on seed 17; eighteenth round; Chat 7/6/7 and 8/6/7 on arm 4, seeds 41 and 19, the boss's fort a lottery at 31 to 47), the ford group a tutorial and the boss's staging free; content in #131, blocked on #158; not tuned |
| the_tollgate | format sample (seize); fails the Fun Gate from both sides (Code 2/4/5 on seed 31, Chat 3/4/4 on seed 37; twenty-third round): 6,4 answers the keep for free, the boss never swung on either play, hand lines 6 and 8 against 16; content in #181, blocked on #158; gate 1 passes at 99 percent and gate 4 fails the cast on the ceiling line (five deployed behind one contested tile); Pell's 0.440 is the captain alone refusing a 28 percent death with a dressing in hand in all 89 stalls (#147), the baseline's heal policy more than a measure of him; not tuned |

## Standing notes

- Owner is hands-off by design. Decide, record, proceed. Detail lives in the merged PR, `docs/DECISIONS/`, and `docs/DIALOGUE.md`; keep "Where we are" to five lines.
- Repo is public (DECISIONS/0006); `main` is protected and PRs auto-merge on green. Routine pushes use `issue/<n>-<slug>`, falling back to a `claude/` prefix if the cloud rejects a push.
- Routines: Builder nightly at 02:00, 03:00, and 05:00 New York, each working the queue for about 50 minutes, plus a chained run of exactly one issue after each merged Builder PR while the chain is on (ROUTINES.md section 6); Critic on the schedule Lotus sets (last recorded Wednesday and Sunday at 04:00; its pass of 2026-09-25 fired on a Friday, so the `critic` issues are the record); Partner and Chat woken by Table comments (DECISIONS/0007, 0009). While the repo variable `IRONWAKE_CHAIN` is `on`, the merge of a Builder PR fires the next Builder run, which does one issue and exits (ROUTINES.md section 6); the workflow stands down while another open issue took `in-progress` under an hour ago, and the Builder prompt is asked to do the same, so the slots and the chain do not work the same queue at once (#154). Code runs on Fable 5.1 on the desktop and as the Partner and on Opus 5.5 as the Builder; the Critic runs on Fable 5.1 and Chat on Opus 5.5; all by Lotus's ruling (DECISIONS/0009). Failure handling is at the end of ROUTINES.md.
- Sim: `--smoke` (CI, gates 5 to 8), `--full <map> [--seeds N] [--scheme one|two]` or `--full --all` (all eight rows, 200 seeds, about a minute per map in Debug), `--trace <map> <seed> [--scheme one|two] [--formula standard|arm1..arm4]` (one game as a script the CLI replays under `--strict` with the same flags), `--hitband <map>|--all [--seeds N]` (issue 158's table, a few minutes per map at 200 seeds in Debug; the output of a run is kept under `docs/measurements/`). Gates 1 to 4 run on the content cast and are not in CI.
- Cloud environments: the setup script preinstalls dotnet-sdk-8.0 and gh (snapshot cached). In the cloud, GitHub goes through the built-in GitHub tools and `gh` reports an invalid token; on Lotus's desktop it is the reverse.
- Content JSON shapes: see `ContentLoader` and the starter files. `units/cast.json` is the roster in file order (captain first) with `region`, `personality`, and `hooks`; other unit files hold enemy templates. `rules.json` holds rule constants belonging to no map (`wakeRadius`). `items.json` holds consumables. Stats objects use lower-case keys `hp str mag dex spd lck def res cha`; `modifiers` and `growthModifiers` may omit stats, a unit's `stats` and `growths` must list all nine. Terrain `cost` uses `null` for impassable.
- Map files: DESIGN.md section 10 and DECISIONS/0011. Hand-edited maps must be canonical (`MapFormat.Write` output); a test names the file when one is not. Coordinates are `x,y` from the top-left, 0-based. CLI slots count from 1; the core stays 0-based.
- CLI and Sim tests read the console only through `ConsoleCapture.Run`, which normalises line endings, and every such test class carries `[Collection("console")]` (#132). Every project runs under invariant globalization, so numbers print the same on every locale. CI runs the required `ci` job on Ubuntu and `ci-windows` on Windows; the Windows job joins the required checks from Lotus's desktop once it has been green for a day.
